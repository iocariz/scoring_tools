# Allocation what-if — constraint narratives

### Target 1.1000%

## Allocation constraint narrative

- **Risk target (input):** 1.1000% (production-weighted portfolio b2 ceiling used by the MILP/greedy solver).
- **Achieved global risk:** 1.0996% | **Global production:** €267,599,982 | **Method:** exact.

**Configured segment limits** (from `segments.toml` / API):
  - **conso_known:** min risk ≥ 0.7000%; max risk ≤ 1.4000%
  - **new_nofintonic_ob:** min risk ≥ 0.8000%; max risk ≤ 1.3000%
  - **no_premium_ab:** min risk ≥ 0.8000%; max risk ≤ 1.4000%
  - **no_premium_cd:** min risk ≥ 0.8000%; max risk ≤ 1.6000%
  - **no_premium_ef:** min risk ≥ 0.8000%; max risk ≤ 1.5000%
  - **premium:** min risk ≥ 0.8000%; max risk ≤ 1.4000%

**Binding constraints:** None within tolerance (MILP slack; greedy uses heuristic flags only).

### Target 1.2000%

## Allocation constraint narrative

- **Risk target (input):** 1.2000% (production-weighted portfolio b2 ceiling used by the MILP/greedy solver).
- **Achieved global risk:** 1.1998% | **Global production:** €280,535,902 | **Method:** exact.

**Configured segment limits** (from `segments.toml` / API):
  - **conso_known:** min risk ≥ 0.7000%; max risk ≤ 1.4000%
  - **new_nofintonic_ob:** min risk ≥ 0.8000%; max risk ≤ 1.3000%
  - **no_premium_ab:** min risk ≥ 0.8000%; max risk ≤ 1.4000%
  - **no_premium_cd:** min risk ≥ 0.8000%; max risk ≤ 1.6000%
  - **no_premium_ef:** min risk ≥ 0.8000%; max risk ≤ 1.5000%
  - **premium:** min risk ≥ 0.8000%; max risk ≤ 1.4000%

**Binding constraints:** None within tolerance (MILP slack; greedy uses heuristic flags only).

### Target 1.3000%

## Allocation constraint narrative

- **Risk target (input):** 1.3000% (production-weighted portfolio b2 ceiling used by the MILP/greedy solver).
- **Achieved global risk:** 1.2999% | **Global production:** €289,164,473 | **Method:** exact.

**Configured segment limits** (from `segments.toml` / API):
  - **conso_known:** min risk ≥ 0.7000%; max risk ≤ 1.4000%
  - **new_nofintonic_ob:** min risk ≥ 0.8000%; max risk ≤ 1.3000%
  - **no_premium_ab:** min risk ≥ 0.8000%; max risk ≤ 1.4000%
  - **no_premium_cd:** min risk ≥ 0.8000%; max risk ≤ 1.6000%
  - **no_premium_ef:** min risk ≥ 0.8000%; max risk ≤ 1.5000%
  - **premium:** min risk ≥ 0.8000%; max risk ≤ 1.4000%

**Binding constraints:** None within tolerance (MILP slack; greedy uses heuristic flags only).

