# Allocation what-if — constraint narratives

### Target 2.0000%

## Allocation constraint narrative

- **Risk target (input):** 2.0000% (production-weighted portfolio b2 ceiling used by the MILP/greedy solver).
- **Achieved global risk:** 1.9998% | **Global production:** €479,422,253 | **Method:** exact.

**Configured segment limits** (from `segments.toml` / API):
  - **no_premium_ab:** min risk ≥ 0.8000%; max risk ≤ 1.4000%

**Binding constraints:** None within tolerance (MILP slack; greedy uses heuristic flags only).

### Target 2.5000%

## Allocation constraint narrative

- **Risk target (input):** 2.5000% (production-weighted portfolio b2 ceiling used by the MILP/greedy solver).
- **Achieved global risk:** 2.0663% | **Global production:** €482,474,091 | **Method:** exact.

**Configured segment limits** (from `segments.toml` / API):
  - **no_premium_ab:** min risk ≥ 0.8000%; max risk ≤ 1.4000%

**Binding constraints:** None within tolerance (MILP slack; greedy uses heuristic flags only).

### Target 3.0000%

## Allocation constraint narrative

- **Risk target (input):** 3.0000% (production-weighted portfolio b2 ceiling used by the MILP/greedy solver).
- **Achieved global risk:** 2.0663% | **Global production:** €482,474,091 | **Method:** exact.

**Configured segment limits** (from `segments.toml` / API):
  - **no_premium_ab:** min risk ≥ 0.8000%; max risk ≤ 1.4000%

**Binding constraints:** None within tolerance (MILP slack; greedy uses heuristic flags only).

