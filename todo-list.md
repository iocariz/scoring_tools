# Audit Findings — Remaining To-Do

Findings from the codebase audit (errors & methodological mistakes). Severities and
`file:line` references are from the audit; a few are design choices flagged for
confirmation rather than definite bugs. Verify before changing behavior — several
items shift headline risk/production numbers.

**Status legend:** `[ ]` open · `[x]` done

---

## Production-Hardening Milestone

**Goal:** raise the codebase from "production-grade software" to "production-grade credit-decisioning system" — i.e. trustworthy enough to drive live cutoffs and survive independent model validation. The engineering hygiene is already strong (≈1,130 tests, ruff, CI, typed/validated config, MR + PSI/CSI + bootstrap diagnostics). The gap is methodological correctness in decision-critical paths and decisioning governance: bugs here don't crash, they silently bias the risk/production numbers that set lending cutoffs.

Exit criteria:

- [ ] **M1 — Clear decision-critical HIGH findings.** Resolve the open Tier-1 items that touch numbers feeding cutoffs, model training/selection (#6, #7, #8 done) and the RI/aggregation logic (#4, #5 done) — all cleared. Each fix verified on real data with the before/after delta reported.
- [ ] **M2 — Data quality becomes a hard gate (#18).** Date-coverage gaps/parse failures, negative counts/amounts, and per-segment booked-ratio checks FAIL the run (not warn) by default; add a documented `--allow-dq-warnings` escape hatch for analysts. Capture the data snapshot/extraction date + basic lineage with each run.
- [ ] **M3 — Config-complexity audit.** Inventory which features are load-bearing vs. experimental; set safe defaults; ship a documented "simple mode" with the fragile features (RI optimizer, MR auto-extrapolation, monotonicity relaxation, optimization binning) off by default. Rationale: the live config stacks many interacting knobs on exactly the most fragile paths, and two live-config footguns (`method="optimization"`, `reject_include_all_rejections=true`) were already found and fixed.
  - Tooling: `config_sensitivity.py` runs a one-at-a-time sweep and ranks knobs by headline impact (production %, risk %, accepted-cell changes). Risk is held ~constant by the fixed risk target, so sensitivity surfaces as **production**.
  - **Full-registry result (`no_premium_cd`, 35 runs, run_ri_optimizer off, baseline €52.5M / 1.09% / 157 cells):**
    - Tier 1 — material: `stress_mode` ±29.4%, `multiplier` ±22.2%, `reject_parceling_method` ±9.9%, `reject_uplift_factor` ±8.7%.
    - Tier 2 — minor (<1%): `reject_bayesian_prior_strength`, `reject_decay_half_life`, `reject_max_risk_multiplier`, `pareto_n_points`, `per_bin_tasa_fin`.
    - Tier 3 — **zero effect** (bit-identical output): `multiplier_h3`, all six MR knobs, `monotonicity_relaxation` + `mono_z_threshold`, `ri_calibration_gamma`/`run_ri_optimizer` (last two are artifacts of run_ri_optimizer=off in the demo config).
  - **Optimizer-on partial sweep (`run_ri_optimizer=true`, ri trials reduced):** toggling the optimizer on drops baseline €52.5M→€42.5M (±19%); `ri_calibration_gamma` ±24% (now the active RI lever); `multiplier` material; `reject_parceling_method` ±5%. **Key interaction:** with the optimizer ON, `reject_uplift_factor` and `reject_max_risk_multiplier` are bit-identical inert (the optimizer overrides them). So the RI knobs are mutually exclusive by mode.
  - **Tier classification:**
    - **Core** (must set, high impact): `variables`/`inference_variables`/`bins`/`directions`, dates, `data_path`, `segment_filter`, `multiplier` (±22%), `optimum_risk`/`risk_step`.
    - **Tuning** (governed, material): `stress_mode` (±29%, biggest lever), `reject_inference_method`/`reject_parceling_method` (±10%), `run_ri_optimizer` (toggle ±19%).
    - **Tuning, mode-dependent:** optimizer **off** → `reject_uplift_factor`/`reject_max_risk_multiplier` (±9%); optimizer **on** → `ri_calibration_gamma` (±24%) + `ri_*_range`. The other set goes inert.
    - **Expert / default-off** (fragile, low/zero impact): MR suite (validation-only, 0 cutoff impact — M3a), `monotonicity_relaxation_enabled`/`mono_z_threshold` (inert), `reject_bayesian_smoothing`/prior/decay (<1%), `mr_extrapolation_*`, `multiplier_h3`, swap-in constraints.
    - **Deprecated:** `reject_include_all_rejections` (#3), `octroi_bins`/`efx_bins` (legacy), binning `method="optimization"` (#1).
  - **Defaults finding:** the Pydantic defaults in `config.py` are ALREADY the simple/safe path — every fragile feature defaults off. The only non-minimal default is `stress_mode="global"` (a deliberate risk-conservatism choice), whose one footgun (stress×parceling double-counting) already has a runtime guardrail (`pipeline/preprocessing.py:117`). So no default changes were needed; tier classification is documented inline in `config.py`. The complexity is in `config.toml` (kitchen-sink), not the code defaults.
  - **Done:** shipped `simple.toml` — a minimal Core-only starting template (2-var grid via modern `BinConfig`, all fragile features omitted → safe defaults; RI/optimizer/MR/smoothing off, stress disabled). Validated end-to-end (no_premium_cd → €55.9M / 1.088%). `config.toml` is the "advanced/full" example.
  - **Remaining:** re-run Tier-1 with full Optuna trials and on a second segment to confirm generality; widen the 2-point grids on Tier-1 knobs.
- [x] **M3a — Confirmed: MR does not feed the optimizer (validation-only).** The Tier-3 zero-impact of every MR knob is by design, not a wiring bug. `process_mr_period` is invoked only in `scenarios.py` (`run_scenario_analysis`), *after* `run_optimization_phase` builds the Pareto frontier and *after* `opt_sol` (the chosen cutoffs) is written; it receives the already-selected cutoffs as inputs (`optimal_solution_df=opt_sol`, `mask=selected_mask`) and its return value is discarded — it only writes `mr_*` validation CSVs. Implication: the MR subsystem (and its audit findings, e.g. #2) affects **monitoring accuracy**, not live cutoffs — so those findings can be re-prioritized below anything that touches the optimizer. Action for M3: document MR explicitly as a diagnostic/validation point and default its knobs to a simple, stable config.
- [ ] **M4 — Independent out-of-time backtest of cutoffs.** Backtest proposed cutoffs against a held-out period (beyond the existing MR check); compare realized vs. predicted risk/production. Make this a repeatable artifact, not a one-off.
- [ ] **M5 — Validation & reproducibility trail.** Someone who didn't write the pipeline attempts to reproduce and break the headline numbers; document assumptions (multiplier, gamma, maturity anchor, stress mode), pin the data snapshot, and record a model-risk-management style sign-off. Required before the tool stands in front of a regulator or drives cutoffs automatically.

**Current stance:** fine to run **today as analyst decision-support with a human in the loop**; do not let it set live cutoffs automatically until M1–M5 are met. Note: the audit list below is a prioritized hypothesis set, not gospel — 2 of the original HIGH findings (#2, #3) were overstated on close reading, so confirm each before acting.

---

## Done
- [x] **#1 — Bin-edge target leakage + wrong population.** `learn_optimization_bins` deprecated (forced quantile); edges now learned on the date-filtered demand population in all 3 call sites (`preprocess_improved.py`, `run_batch.py`). Verified: income edge shifted −20.8% (booked→demand) on real data.
- [x] **#2 — MR maturity anchored to `max(mis_date)`.** H3 path (`mr_pipeline.py:410`) and `_assign_tiered_risk` (`:939`) now use the observation-horizon anchor (`date_fin_book_obs_mr`), consistent with the H6 path. (H6 path was already correct given the end-of-month-next-month data convention.)
- [x] **#3 — RI calibration / acceptance-rate population mismatch.** Root cause was `reject_include_all_rejections=true`: the all-rejects acceptance rate fed both the parceling uplift and the calibration target, but the blend is only score-rejected. Deprecated the flag and forced **score-only** acceptance rates everywhere (`reject_inference.py:compute_acceptance_rates`); fixed the misleading warning that recommended the flag. Original "wrong estimand" framing was overstated — downgraded from HIGH; the objective's intent (blend ≈ selection-model population risk) is sound.
- [x] **#4 — Bayesian/empirical-Bayes smoothing mixes scales under time decay.** `reject_inference.py:compute_acceptance_rates`. Under time-decay the Beta-Binomial posterior was computed on `Σw` (float effective counts shrunk below the raw count), so the count-scale prior systematically over-shrank toward the global rate, and the empirical-Bayes moment estimator (unweighted cross-bin variance, global `p`, `n_i = Σw`) was unsound on a few weighted bins. Fix (Option 2, domain-expert-chosen): under decay the posterior is now computed on the **Kish effective sample size** `n_eff = (Σw)²/Σw²` using the analyst-configured `bayesian_prior_strength` directly, and the EB auto-tuning is skipped — it now runs only on the unweighted integer-count path where it is valid. The no-decay path is **bit-identical** (all existing tests green; added 6 decay-specific tests; the no-decay regression golden was strengthened from a degenerate single-bin case — where `global_rate == bin_rate` makes smoothing a no-op — to a 2-bin case that actually exercises the EB auto-tune). **Real-SAS before/after** (segment `no_premium_cd`, smoothing+decay on, RI optimizer off to isolate the fix, identical trained model): swap-in reject multiplier avg 2.252 → 2.277 (+1.1% rel, less over-shrinkage toward the low global accept rate); optimum production €51,832,851 → €51,948,449 (**+€115.6K, +0.223%**), accepted cells 157 → 160 (+3), optimum risk 1.0900% → 1.0995% (+0.0095 pp, both under the 1.1% cap). Sub-1% headline impact, consistent with the M3 sensitivity finding for the decay/smoothing knobs.
- [x] **#5 — No-demand cells get the *smallest* RI uplift.** `reject_inference.py:apply_parceling_adjustment`. Repesca bins absent from the acceptance-rate table (zero demand = highest selection bias) were filled with the *median* observed rate → near-typical, anti-conservative multiplier. Fix (Option 3, domain-expert-chosen): **confidence-weighted shrinkage** toward a conservative low anchor — `rate_eff = conf·bin_rate + (1−conf)·anchor`, `conf = 1−exp(−n/scale)`, `anchor = max(low-percentile of observed rates, 0.01)` (fixed 0.05 if none). No-demand bins (conf 0) collapse to the anchor (high uplift); low-demand bins partially shrink (also fixes the adjacent over-trust of sparse bins, #37/#81); well-observed bins (conf ≈ 1) are ~unchanged. Shared `_shrink_acceptance_rate` helper is used by both `apply_parceling_adjustment` (runtime) **and** the RI optimizer's `_compute_calibration_error`, so the calibration objective scores candidates on the same rate definition runtime applies (lockstep — the optimizer had its own duplicate median-fill). Two config knobs `reject_no_demand_anchor_percentile` (0.10) / `reject_confidence_scale` (10.0); new `ri_effective_acceptance_rate` diagnostic. **Real-SAS before/after** (segment `no_premium_cd`, smoothing+decay on, RI optimizer off to isolate, identical trained model): no no-demand bins fired, so the effect is purely the low-demand de-trusting → swap-in reject multiplier avg 2.277 → 2.348 (+3.1% rel), swap-in risk 2.543% → 2.616%; optimum production €51,948,449 → €51,825,568 (**−€122.9K, −0.237%, more conservative**), accepted cells 160 → 157 (−3), optimum risk ~1.099% (unchanged, under cap). Sub-1%, consistent with M3.
- [x] **#6 — HurdleRegressor / zero-inflation rationale is inert.** `estimators.py`, `inference_optimized.py`, `optuna_tuning.py`. The two-part hurdle was trained on the bin-aggregated continuous `b2_ever_h6` (≈ never exactly 0), so the classifier learned all-ones and it collapsed to plain Ridge/Lasso while still offered as a distinct candidate; the zero-proportion diagnostic was measured post-aggregation (≈0) and never used. Fix (Option 3, domain-expert-chosen — make it meaningful): the hurdle is now trained on **per-loan** data where the default indicator has real zero mass, via a shared `_fit_hurdle_per_loan` adapter (per-loan `_hurdle_r` target + exposure weight `_hurdle_w`), and scored on the same bin-level CV RMSE as peers (same CV split → fair, no leakage). Exposure-weighted severity makes the cell prediction reconcile to the exposure-weighted aggregate `b2 = mult·Σnum/Σden` (exact in the no-shrinkage limit). Diagnostic now per-loan (`todu_30ever_h6<=0`). Gated behind `model_hurdle_per_loan` (**default off**) + a degenerate-zero-mass skip; default runs offer no hurdle (retires the inert candidate + 2 wasted Optuna tunings). **Real-data validation** (`no_premium_cd`, flag OFF vs ON): per-loan zero mass = 98.8% (vs the misleading post-aggregation ≈0); the per-loan hurdle now competes for real (Hurdle-Ridge CV RMSE 0.0206, classifier modeling the ~1.2% default rate) but **loses to LightGBM (0.0178)** → both runs select LightGBM, so the optimum is **IDENTICAL** (risk 1.0995%, production €51,948,449, 160 cells) — zero cutoff impact, and the surface-monotonicity risk never triggers (the hurdle doesn't win). Enabling in production remains an operational decision (M5 sign-off).
- [x] **#7 — Model selection reuses one holdout; 1-SE rule degenerates.** `inference_optimized.py`, `optuna_tuning.py`. Every model-type candidate (linear families AND trees) was scored on the **same** 0.2 holdout (same `random_state`), so all rows reported `CV Std RMSE = 0` → the 1-SE band (`best_mean + best_se`) collapsed to pure **argmin** (the complexity tiebreaker never fired), and that one reused holdout both *selected* the winner and reported its "unbiased" RMSE (winner's-curse bias). Fix (Option 1, domain-expert-chosen): score each candidate's tuned params with **fresh-seed k-fold CV** (mean + real SE, a different fold partition than tuning) → the 1-SE rule forms a proper band and prefers the simplest model within noise; the 0.2 holdout is kept **only** as a winner-only generalization report (`evaluate_holdout_rmse`, refit on 80% / scored on 20%, never used for selection; logged + in metadata). Deleted the per-tuning-function holdout + `eval_holdout_tree`/`_eval_holdout_linear`. **Real-data before/after** (`no_premium_cd`): the CV table now carries real SE (~0.0006–0.0008 vs the old 0.0); all candidates fall within LightGBM's 1-SE band, so selection flips **LightGBM → Linear Regression** (the simplest, statistically-tied model; winner held-out RMSE 0.0171). Optimum **1.0991% → 1.0899%**, production **€51.83M → €49.58M (−4.34%)**, accepted cells 157 → 129. **Material** (model flip + −4.3% production) — the largest of the Tier-1 RI/selection fixes and the correct, more-conservative effect of a working 1-SE rule; **requires M1/M5 sign-off before the new model drives live cutoffs.**
  - **Production (multi-segment) validation (2026-05-31): the −4.3% headline was a single-segment-training artifact, not the production impact.** In production all segments share ONE model trained on the pooled `total` supersegment (8 segments, ~50k bins), not the cd-only slice the headline was measured on. Re-running the *real* pooled training under pre-#7 (`767c708`) vs current code: pre-#7 (broken 1-SE, all CV-Std=0 → argmin) selects **Lasso α=0.001**; current (fresh-seed k-fold 1-SE) selects **Linear Regression** — **both linear, functionally near-identical** (α≈0 Lasso ≈ OLS). The alarming **LightGBM** pick was specific to the tiny cd-only training set and does **not** occur on pooled data. Base-scenario optima with each pooled model (segments ef→cd→ab chain + premium): no_premium_ef −2.93% (−€179K, −6 cells), no_premium_cd −0.88% (−€453K, −13 cells), no_premium_ab **0%** (fixed_cutoffs ⇒ production pinned; risk metric shifts ~+0.007pp), premium **0%** (optimum identical). **Aggregate −€632K, −0.416%**, all risk targets at/under cap, direction marginally more conservative. Conclusion: #7's production effect is a small, conservative shift between two near-equivalent linear models — **not** a regime change to a tree model. Lowers the operational risk of the original "material" flag (M1/M5 sign-off still advisable, but the live-cutoff impact is sub-1% in aggregate).
- [x] **#8 — TweedieGLM uses a penalized free `log(exposure)` coefficient, not a fixed offset.** `estimators.py`, `optuna_tuning.py`. The Tweedie converted the rate target back to the default amount and appended `log(exposure)` as a feature — but sklearn's `TweedieRegressor` L2-penalizes every coefficient, so the "offset" was a penalized free coefficient (shrunk off 1.0), not a true offset; and the same `todu_amt_pile_h6` was used as BOTH that exposure AND the `sample_weight` (double-counting), plus a predict-time median-exposure fallback. Fix (Option 1, domain-expert-chosen): model the rate `b2` directly with `sample_weight = exposure` — sklearn's documented exposure idiom, mathematically equivalent to `offset=log(exposure)` for the mean (exact for Poisson; ~exact for Tweedie 1<p<2). Exposure now used exactly once (as the weight); removed `exposure_col`/`multiplier` + `_split_exposure`/`_add_log_exposure`/`_median_exposure` + the exposure branches; `prepare_model_input` no longer appends an exposure column. Net simplification, consistent with how Ridge/Lasso/LightGBM model the rate. **Real-data before/after** (`no_premium_cd`): Tweedie CV RMSE 0.020587 → 0.020400 (corrected fit, marginally better); selected model **Linear Regression in both** (Tweedie doesn't win the 1-SE selection post-#7) → optimum **IDENTICAL** (1.0899%, €49,575,119, 129 cells) — **zero cutoff impact** on this segment.
- [x] **#9 — Cell-level CIs are too narrow.** `inference_optimized.py:compute_cell_level_ci`. Per-cell uncertainty used `SE = std(fold_preds)/√k`, treating cross-fold predictions as independent — but folds share training data, so they are positively correlated (the Nadeau–Bengio CV-variance pathology) and `/√k` underestimates the SE ~2×. Fix (Option 1, domain-expert-chosen): new `_nadeau_bengio_cv_se(std, n_folds)` helper returns the corrected `SE = std·√(1/k + 1/(k−1))` (`std/√k` guarded fallback below 2 folds); `compute_cell_level_ci` now uses it, keeping the `t(df=k−1)` critical value, the raw `pred_std` column, and the `<2-fold → NaN` branch. **Scope (verified): these CIs are report-only** — no decision path consumes them (not the optimizer, monotonicity relaxation, scenario selection, or any cutoff rule); they feed the dashboard heatmap/CSV display only. The cutoff-level bootstrap (`calculate_bootstrap_intervals`) and consolidation aggregation CIs are separate and unchanged. **Real-data before/after** (`no_premium_cd`, 163/180 cells with a CI): `pred_mean`/`pred_std` bit-identical (same deterministic model); CI half-widths widen by exactly the NB/naive factor `√(1 + k/(k−1))` per cell's fold count — k=2 ×1.732 (23 cells), k=3 ×1.581 (54), k=4 ×1.528 (86); mean half-width 0.00604 → 0.00987. Optimum **IDENTICAL** (1.0899%, €49,575,119, 129 cells) — **zero cutoff impact**, just more honest (wider) cell uncertainty in the display.
- [x] **#10 — SPC control limits not estimated from within-subgroup variation.** `trends.py:detect_trend_changes`, `main.py:601`. The control-band σ was the rolling **MAD of the raw levels** (`rolling_mad/0.6745`), which captures the full within-window dispersion *including any trend* — so under a slow drift the spread inflated, the band widened, and anomalies riding the drift were **masked**. Compounded by a tertiary **global-std** fallback (worst estimator under drift), a small-window **t-quantile × per-point-spread** branch (at `window=3`, df=2 → band explodes), and `main.py` calling it with **`window=3`** (2 moving ranges). Fix (Option 1, domain-expert-chosen): replace with the robust **Individuals moving-range (I-MR)** estimator `σ̂ = median(|x_i − x_{i-1}|)/0.9539` — within-subgroup short-term variation, drift-immune; keep the lagged robust-median centre and the no-leakage `.shift(1)`; use a plain Shewhart `n_sigma` multiplier (dropped the t-quantile branch **and** the global-std fallback); raise the production window **3 → 6**. Median-MR (not mean-MR/1.128) to resist a single outlier month on short (~12-pt) series and match the file's robust style. Methodology doc bullets updated. **Scope (verified): report-only** — `is_anomaly`/`direction` flow only to a CSV + WARNING log (non-blocking) + the HTML "Anomalies" table; no optimizer/monotonicity/scenario/cutoff path consumes them. **Real-data before/after** (`no_premium_cd`, 37 months): mean band half-width **0.1158 → 0.0273** and far more stable (old window=3 bands swung 0.005–0.257; new sit in 0.013–0.031); the drift-masking bug is demonstrably fixed — 2024-02 (0.109) & 2024-03 (0.114) had old half-widths of **0.257** (drift-inflated) ⇒ masked, now flagged; 2024-12 similarly surfaced; spurious window=3 flags (2023-08/10, 2025-02/03) drop out. Optimum **byte-IDENTICAL** (1.089877%, €49,575,118.89, 129 cells) — **zero cutoff impact**. 3 new tests (drift-masking regression, constant-series, MR-σ unit) + 1164 pass.
- [x] **#11 — PSI "non-standard" epsilon-only-in-log variant — OVERSTATED, verified defensible (no formula change).** `stability.py:calculate_psi`/`calculate_csi_for_categorical`, `metrics.py:calculate_psi_by_period`. PSI = `Σ (a−e)·ln(a_safe/e_safe)` with epsilon flooring only the log term (difference keeps true proportions, no re-normalization). The audit's specific worry — *asymmetric weighting of appearing vs disappearing bins* — **does not hold**: PSI is invariant under swapping the two populations (`PSI(a,e)=Σ(a−e)ln(a/e)=PSI(e,a)`), and swapping turns every appearing bin into a disappearing one, so the two are weighted **identically** (numerically confirmed: appearing-mass-p == disappearing-mass-p, e.g. 0.6215==0.6215 at p=0.05). It also equals the textbook epsilon-in-**both**-terms PSI to **O(ε)** — diff ≈ `ε·ln(p/ε)` per empty bin (~1.5e-3 in tests), far below the 0.1/0.25 thresholds, which therefore remain valid; and every term is ≥0 (PSI≥0). Fix (Option A, domain-expert-chosen): **keep the formula**, encode the verification — added `test_psi_symmetric_under_population_swap`, `test_psi_matches_textbook_within_epsilon`, `test_csi_symmetric_under_population_swap`, and a docstring note in all three functions. **Report-only** (PSI/CSI → CSV/JSON/HTML + INFO/WARNING logs + alert severities; `mr_pipeline` even downgrades errors to warnings; no optimizer/cutoff path consumes them). No formula change ⇒ **PSI values byte-identical, zero cutoff impact**; no real-data run needed. Existing golden/empty/threshold tests unchanged & green; suite 1167 pass. (Mirrors #2/#3: original HIGH framing overstated on close reading.)
- [x] **#12 — PSI silently returns ~0 for low-cardinality scores.** `stability.py:calculate_psi`/`get_psi_status`, `metrics.py:calculate_psi_by_period`. `pd.qcut(..., duplicates="drop")` silently collapses to a single bin on a low-cardinality / dominant-value score (it does **not** raise, so the `except ValueError → equal-width` fallback never fired); one bin makes `PSI=(1−1)·ln(1)=0` regardless of any real shift → false **"stable"**. Fix (Option A, domain-expert-chosen — *measure it properly; NaN only when constant*): the int-bins path now branches on baseline cardinality — `nunique<2` (constant reference) → **NaN + warning** (never silent 0); `nunique<=bins` (discrete) → **one bin per distinct value** (a real discrete-support PSI); else `qcut`, and a silent `<2`-bin collapse → **equal-width fallback** (NaN if still degenerate); every fallback logs a warning. `get_psi_status(NaN)` now explicitly → `UNSTABLE` (surfaced for investigation, never silently STABLE). Parallel hardening in `metrics.calculate_psi_by_period`: `np.unique` the percentile breakpoints (low cardinality made `np.histogram` crash on duplicate edges) + NaN guard. **Report-only** (no optimizer/cutoff path consumes PSI). **Real-data before/after** (`no_premium_cd`): the monitored continuous vars (`oa_amt`/`risk_score_rf`/`score_rf`) take the unchanged qcut path → `stability_psi*.csv` **byte-identical** (PSI 0.0775/0.0512/0.0064), optimum **byte-IDENTICAL** (1.089877%, €49,575,118.89, 129 cells) ⇒ **zero cutoff impact**; the new low-card behavior is exercised by 4 new unit tests (discrete-shift detected, dominant-value fallback, constant→NaN, metrics no-crash). Suite 1171 pass.
- [x] **#13 — global_optimizer Pareto pruning unified on the canonical sweep (was: "fragile").** `global_optimizer.py:load_frontier`, `optimization_utils.py`. Two Pareto-pruning implementations existed: `load_frontier` used `cummax(oa_amt_h0)` + a hard-coded **1e-4** epsilon; `trace_pareto_frontier` used the canonical strict `prod > prev_max` (no epsilon). Same rule modulo the epsilon — and since production is in **euros (~10⁷)**, a sub-1e-4 (0.01-cent) gap never occurs between distinct solutions, so the epsilon **never fired** on real data. Fix (Option A, domain-expert-chosen): extracted the canonical rule into a shared `pareto_keep_indices(production_sorted_by_risk)` helper and call it from **both** — `trace_pareto_frontier` (pure refactor, identical logic ⇒ per-segment pipeline unchanged) and `load_frontier` (drops the cummax/1e-4 mask). Single source of truth, magic number gone. The global frontier **is** the allocation-MILP candidate set (first non-report-only fix), but **real-data `run_allocation` before/after** (11 segments @ target 1.2%, multi-segment `output/` frontiers): per-segment pruned point counts **identical**, global risk **1.1997%**, global production **€297,093,790**, and the allocation CSV + policy-cutoff table (chosen `sol_fac`/risk/production/cells/swap per segment) are **byte-IDENTICAL** ⇒ **zero allocation impact**. 3 existing pruning golden tests stay green; +1 documents the new strict-`>` (sub-epsilon gain now kept) +6 `pareto_keep_indices` unit tests. Suite 1178 pass. (Audit's "fragile" overstated — the old filter was correct; this is a consistency/maintainability fix.)
- [x] **#15 — MILP decode could report an infeasible allocation as optimal.** `global_optimizer.py:optimize_exact`/`optimize_greedy`/`AllocationResult`, `run_allocation.py`, `portfolio_owner.py`. After `scipy.milp` succeeded, the decode picked each segment via `argmax(seg_x)`; on a degenerate/fractional result (`round(seg_x).sum() != 1`) it only warned and still took the argmax, **never re-checking** the chosen rows vs the global risk target / per-segment bounds, and returned an `AllocationResult` with **no feasibility field** → infeasible presented as optimal (and binding-constraint slack was computed on the *fractional* solver vector, masking it); `optimize_greedy` likewise could stop with `risk > target` and return silently. Fix (Option A, domain-expert-chosen — validate+raise+honest flag end-to-end): new `_check_allocation_feasible()` re-validates the **decoded** integer choices against every MILP constraint (global production-weighted risk ≤ target, per-segment min/max risk + min production, global production floor); `optimize_exact` **raises** on violation → routed through `optimize()`'s existing greedy fallback; `AllocationResult` gains `feasible`/`message` set by **both** methods (greedy now honestly labels its best-effort instead of silent `risk>target`); binding constraints recomputed from the decoded one-hot; `run_allocation` logs a loud WARNING and the portfolio-owner narrative carries an `⚠ INFEASIBLE` line. **Correctness guard** (the argmax path is rare since scipy returns clean integers on success), allocation-path but **not** report-only. **Real-data `run_allocation` before/after** (`output/`, 11 segments): **feasible** target 1.2% → allocation CSV + policy-cutoff table **byte-IDENTICAL**, `feasible=True` (zero impact on normal runs); **infeasible** target 0.3% → exact infeasible → greedy fallback (best 1.0192%) now **flagged `feasible=False`** with a loud WARNING + narrative INFEASIBLE line (was silently presented as optimal). +5 `_check_allocation_feasible` unit tests, +2 greedy-flag tests, existing exact/greedy/locking tests carry `feasible` asserts; suite 1185 pass.
- [x] **#16 — Monotonicity-direction inference mis-calibrated + wrong default.** `preprocess_improved.py:_infer_monotonicity`. For each variable **without** an explicit `[preprocessing.directions]` entry, it inferred risk's ascending/descending direction by `sign(weighted_cov)` **only when a permutation test was significant** — and on non-significance (p>0.10) or cov≈0/NaN it **discarded the empirical sign and forced `direction=1` (ascending)**, backwards for credit scores (higher score = safer = `-1`). The permutation also held production **weights fixed** while shuffling risk ranks → mis-calibrated p-value that deemed genuinely-descending-but-noisy scores "not significant," amplifying the wrong default. Direction feeds `inv_vars` → a **hard MILP monotonicity constraint**. Fix (Option A, domain-expert-chosen — empirical sign always + calibrated warning): new pure `_infer_direction_from_bins()` sets `direction = sign(rho_w)` of a coherent production-weighted rank correlation **regardless of significance**; the permutation (now permuting the **(risk_rank, weight) pairs jointly** → exchangeable null) is **confidence-only** and never overrides the sign; a flat/degenerate profile (`rho_w` NaN/≈0) → documented ascending default + loud warning. **Blast radius:** inference is **dormant** when directions are set explicitly (standard production path). **Real-data on `no_premium_cd`:** (a) standard config (dirs explicit) before/after → optimum **byte-IDENTICAL** (1.0899%, €49,575,119, 129) — zero impact on normal runs; (b) **directions stripped to force inference** → the fix infers **−1/−1/−1** (correct descending; rho_w −0.47/−0.51/−1.00, all "weak" p>0.10) and reproduces the **explicit-directions optimum** (1.0899%, €49.575M, 129), whereas the **old code forced +1/+1/+1 (ascending)** → backwards constraints → optimization **crashed** (`MILP and GA produced no solutions`). +6 unit tests incl. the noisy-descending regression (old would force +1, new returns −1). Suite 1191 pass. (No `analyze_logs` regex couples to the new colon-formatted log wording.)
- [x] **#17 — RI partial-order monotonicity "fix" was not a valid isotonic projection.** `reject_inference.py:_fix_partial_order_violations`/`apply_parceling_adjustment`. The post-hoc that enforces monotonicity on the per-cell `reject_risk_multiplier` (over the N-D bin poset; gated by `reject_enforce_monotonicity`, **on** in config.toml/ri_verify.toml; multiplier scales `todu_30ever_h6` → optimizer, **not** report-only) **averaged violating pairs** in a `for _ in range(5)` loop — averaging a pair can re-break others, the loop could exit non-converged on deep posets, and it produced an arbitrary monotone *staircase* rather than the L2 projection. Fix (Option A, domain-expert-chosen — block-pooling generalized PAVA): pool whole connected violating **blocks** via union-find to their mean and iterate to convergence — blocks only ever merge ⇒ **guaranteed termination with zero residual partial-order violations** and the correct isotonic surface (e.g. a decreasing chain now pools to its mean, not a staircase). Kept the axis-wise sklearn isotonic warm-start; reordered to **clip-then-enforce** so the output is provably **both monotone and within [1, max_mult]** (a uniform clip is order-preserving — the audit's "clip re-breaks monotonicity" sub-claim was unfounded; noted, not "fixed"). **Real-data before/after** (`no_premium_cd`, monotonicity on): the post-hoc fires on real data (old averaged 17 violating pairs; new pooled 9 blocks) yet the optimum is **byte-IDENTICAL** (1.0899%, €49,575,119, 129) ⇒ **zero cutoff impact** — the old averaging happened to reach an adequate surface here; the fix's value is the guarantee + correctness. +5 unit tests (zero-residual-violation guarantee, chain→isotonic-mean, idempotence, inverted axis, bounded+monotone after clip-then-enforce). Suite 1196 pass.
- [x] **#14 — Global risk production-weighted vs segment risk exposure-weighted — convention surfaced + cross-checked (definitional, no optimizer change).** `global_optimizer.py`, `run_allocation.py`, `portfolio_owner.py`, README. The global allocation optimizes a **production-weighted** average of segment bad-rates (`Σ prod·risk/Σ prod`, risk per euro of new production — the documented intent), while each segment's `b2_ever_h6` is **exposure-weighted** (`Σbad/Σexposure`); the two differ only if production ∦ exposure. Confirmed **documented-intentional**, not a bug (matches the audit's "definitional, confirm with business"). Fix (Option A, domain-expert-chosen): **keep the production-weighted constraint unchanged** (zero cutoff/allocation impact) and additionally compute + report the **exposure-weighted portfolio bad-rate** (`Σ b2·exposure/Σ exposure`, from `todu_amt_pile_h6` already present per frontier point) on `AllocationResult.portfolio_bad_rate`, in `__str__`, `run_allocation`, and the portfolio-owner narrative; documented the convention (README + docstring). None-safe for old frontiers lacking exposure. **Real-data `run_allocation`** (`output/`, 11 segments @ target 1.2%): allocation CSV + policy-cutoff table **byte-IDENTICAL** to `main` ⇒ zero impact; the two bases are **near-identical on this portfolio** — production-weighted **1.1997%** vs exposure-weighted **1.2005%** (0.0008pp gap), confirming the convention difference is immaterial here. +3 unit tests (exposure-weighted reported & differs; allocations unchanged with/without exposure; greedy reports it). Suite 1199 pass.

---

## Tier 3 — Data quality & loading (fail-open / silent coercion)

- [x] **#18 — DQ checks no longer fail open on genuine corruption + booked-ratio denominator fixed.** `data_quality.py`, `config.py`, `pipeline/preprocessing.py`. DQ "failed open": negative counts/amounts and unparseable dates were **WARNING-only** (warnings never halt — the gate stops only on `is_valid=False`/`FAILED`, and the dead `allow_warnings` knob was unwired), and `check_booked_ratio` divided by the **full pre-segment-filter** frame (a *global*, all-segment ratio incl. fraud/legal). Fix (Option A, domain-expert-chosen — fail-closed for corruption + strict knob): negative indicator values (`check_indicator_values`) and date-parse failures (`check_date_range` except branch) → **FAILED** (halt by default); coverage gaps + outliers stay **WARNING** (legitimately variable). `check_booked_ratio` now takes `segment_filter` and computes on the **segment demand population** (regex/supersegment-aware match + `fuera_norma`/`fraud_flag`/`nature_holder!=legal` exclusions). New config **`dq_allow_warnings` (default True)** wired into the preprocessing gate so operators can opt into strict mode (warnings also halt). **Probed real data: clean** (0 negatives across 7 indicators; 100% of `mis_date` parses) ⇒ fail-closing is **zero-impact on current runs**. **Real-data DQ on `no_premium_cd`**: run passes (0 failures), and the booked ratio is now the correct **per-segment 14.2% (20,297/143,269)** vs the old **global 7.0% (101,123/1,444,802)**; DQ only gates (never mutates) ⇒ optimum unchanged. +6 tests (negatives→FAILED, unparseable→FAILED, segment denominator + demand exclusions + regex filter, strict-mode halt). Suite 1204 pass.
- [x] **#19 — String coercion now protects dates/IDs + SAS encoding unified (latent-robustness + consistency; no result change).** `data_manager.py`, `run_batch.py`, `config.py`. Both load paths blanket-coerced every object/string column (`lower`+`_`+`category`). This is **essential** — probed raw SAS is Title/UPPER case (`status_name`=`Booked`, `reject_reason`=`09-SCORE`, `fuera_norma`=`N`, `se_decision_id`=`OK`), and every downstream filter depends on the lowercased form — but the blanket loop **would mangle** a string date (space→`_` breaks `2024-01-01 00:00:00`) or case-sensitive ID. On current data it **corrupts nothing** (`mis_date`=datetime64, `authorization_id`=numeric → not object; `se_decision_id` is string but genuinely a filter), so the audit's "corrupts" was overstated → **latent** risk. Fix (Option A, domain-expert-chosen — protect dates/IDs + unify encoding): shared `standardize_columns_and_values()` helper (used by both `load_and_prepare_data` and `run_batch.load_and_standardize_data`) **skips** `mis_date`/`authorization_id`/any `*_date` from value coercion while still normalizing the categorical filters (`se_decision_id` stays normalized). New config **`sas_encoding` (default `latin-1`)** used by both read sites, replacing `run_batch`'s hard-coded `utf-8` (removes the latin-1/utf-8 split). **Real-data: standardized frame byte-IDENTICAL** main vs branch (filter cols + `mis_date`/`authorization_id` fingerprints all match — protected cols are non-object so the new skip is never reached) ⇒ **zero result impact, optimum unchanged**; and `run_batch`'s load path now produces **identical** standardized data to the main path. +4 tests (filter cols normalized; string-date/ID/`*_date` left verbatim; col-name lowercasing; encoding plumbed from settings). Suite 1208 pass.
- [x] **#20 — Required-column check unified on a shared case-sensitive helper (no false assurance; no result change).** `data_manager.py`, `data_quality.py`. `check_required_columns` (DQ) was **case-insensitive** while `validate_data_columns` (the hard gate) is **case-sensitive** — so DQ could report "all columns present" on mixed-case data that the case-sensitive pipeline (and pandas `df[col]` access) actually rejects (**false assurance**). Both run post-standardization on lowercased names in the live flow, so the divergence was **latent** (only the standalone `validate_data_or_fail`, never called by the pipeline, could hit it). Fix (Option B, domain-expert-chosen — unify): new shared `find_missing_columns(df, required)` (**case-sensitive** exact match — the pipeline's real requirement) backs **both** functions, so "passes DQ" ⟺ "passes validate" by construction and they can't diverge again. **Real-data:** DQ output is **byte-IDENTICAL** main vs branch (Required Columns PASSES on both; the unrelated standalone-config failures are identical) ⇒ **no-op in the live flow**. Behavior change is only that DQ now correctly FAILS on non-standardized mixed-case columns instead of falsely passing. +4 tests (DQ case-sensitive exact-match; `find_missing_columns` present/missing/case-sensitive). Suite 1211 pass.
- [x] **#21 — Out-of-range binning now flags OOD-snapped vs NaN-dropped distinctly (already warned; overstated).** `preprocess_improved.py:_apply_binning_from_config`. `pd.cut` NaNs scores outside the edges; the code snaps below-range→first bin / above-range→last bin (KEPT — these contaminate the boundary cell that feeds the MILP grid) and drops true-NaN-source rows. It **already** logged a WARNING + **hard-fails >1%**, so "silently snapped" was **overstated**. Gap: the warning **conflated** OOD-snapped (kept) with NaN-source (dropped), and no test covered the finite-edge path. Fix (Option A, domain-expert-chosen — clarify the flag, keep behavior): one WARNING now reports `N below-range + M above-range snapped to boundary bins` vs `K NaN-source dropped` separately (precomputed `n_nan_src = nan_count − n_below − n_above`). Snapping + the 1% hard-fail are **unchanged**. **Blast radius: unreachable on the standard config** — `octroi_bins`/`efx_bins` and learned/quantile edges are always `±inf`-capped ⇒ `nan_count==0` ⇒ the block is skipped ⇒ **byte-identical output** (no-op); reachable only via a finite `bin_edges` override. +4 tests exercise the finite-edge path (below→first bin, above→last bin, NaN-source dropped & distinct, >1% raises). Suite 1215 pass. (Mirrors #11/#13/#19/#20: latent / overstated → clarity + test-coverage fix.)

---

## Related sub-findings (lower priority / context)

- [ ] **#2b — Immature zero-H6 rows pass the `.notna()` gate.** `mr_pipeline.py:320`. Mitigated by the #2 anchor fix, but a correct anchor doesn't NaN-out H6=0 immature rows at source. Separate question: should immature H6 zeros be treated as NaN? **LOW.**
- [x] **RI Gini imputation self-fulfilling — labeled as a circular upper bound (diagnostic-only; value unchanged).** `selection_bias.py:compute_ri_gini`, `selection_bias_plots.py`, `run_selection_bias_analysis.py`. Confirmed: rejected bad outcomes are imputed as `Binomial(booked_bad_rate[score_decile]·uplift)` — a monotone function of the **same score** the Gini then ranks by — so `ri_gini_mean` is **mechanically inflated** by construction. Verified **diagnostic-only** (CSV `ri_gini.csv` + the "RI-imputed" plot bar + a summary print; **nothing** consumes it — no cutoff/RI-tuning/model-selection/gate → zero result impact). The docstring already admitted "inherently circular" but the user-facing output didn't warn readers. Fix (Option A, domain-expert-chosen — keep the metric, make the circularity loud): added a `note` field to the returned dict (flows into `ri_gini.csv`), relabeled the plot bar to **"RI-imputed (circular upper bound)"** + a chart footnote, a **CAUTION** line in the summary printout (point to the Thorndike-corrected Gini instead), and strengthened the docstring. **Value unchanged** ⇒ no behavior/cutoff impact. +1 test (result carries a "circular" note); suite 1216 pass.
- [x] **Thorndike `attenuation_pct` negative — clamped corrected ≥ observed (defensive; unreachable for valid inputs).** `selection_bias.py:thorndike_case2_correction`. A Case-2 range-restriction correction (`u<1`) only *raises* the estimate (`r_corr ≥ r_obs` since `denom=sqrt(u²+r²(1−u²))≤1`), so `corrected_gini < observed_gini` → negative `attenuation_pct` should never happen. Investigation found it's actually **unreachable for valid Gini ∈ [0,1]**: the `np.clip(r_corr, 0, 0.999)` ceiling maps to `_r_to_auc(0.999)=1.0` ⇒ `corrected` saturates at **1.0 ≥ any valid observed** (only an invalid `observed_gini>1` inverts) — so the audit's "can be negative" was overstated/latent. Fix (Option A, domain-expert-chosen): added `corrected_gini = max(corrected_gini, observed_gini)` to make the theory invariant explicit + robust (e.g. if the clip bounds change later); `attenuation_pct ≥ 0` follows. **No-op on valid inputs** (corrected already ≥ observed); diagnostic-only (feeds the selection-bias plot/CSV, not the optimizer) ⇒ zero cutoff impact. +1 invariant test (corrected ≥ observed & attenuation ≥ 0 across an observed_gini × u_ratio grid incl. near-saturation). Suite 1217 pass.

---

## Notes
- Confidence: items directly verified during the audit are higher-confidence; the rest come from careful reviews with quoted code at medium-high confidence. **#11 and #13 are likely overstated** (design choices). Confirm Tier-1 methodology items with a domain expert before changing behavior.
- Several fixes (#3–#9, #14) interact with the credit methodology — scope/confirm intent before implementing.
- Quick, high-value hardening: **#10 (SPC), #15 (feasibility assert), #18 (fail-open DQ).**
