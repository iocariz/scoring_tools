# Audit Findings — To-Do

Consolidated findings from three audit passes (first pass 2026-05, second pass 2026-06-10, third pass 2026-08-12). **Open items first, classified by relevance; closed items and milestone history at the end.** Every open item was re-verified still-present in source as of 2026-08-12: the third pass re-verified all overlapping second-pass items directly, and the remaining second-pass lows were spot-checked the same day (none had been fixed in passing). Item numbers are stable across passes. Several items shift headline risk/production numbers — verify on real data before changing behavior.

**Status legend:** `[ ]` open · `[x]` done

**Current stance (M1–M5 all met):** approved to run **as analyst decision-support with a human in the loop**. Automated live cutoffs remain gated on the `reports/validation/mrm_signoff.md` conditions (see Done / History).

---

## Open Findings — by relevance

### ⏸ Deferred — reproducibility items (dataset/product change, 2026-08-12)

The dataset and products changed (`config.toml` now points at `demanda_retail_inftel_out_3`), so the committed M5 references pin a superseded snapshot and reproducibility comparisons are not meaningful until the new dataset/product setup stabilizes. **Deferred on Inigo's call — do not work these until re-validation on the new data is scheduled:**

- [ ] **Governance action — stale M5 reference (deferred).** The old-dataset reference already FAILED on the numbers pre-change (risk Δ +0.0102pp vs 0.01 tol, production Δ +4.82%, 96→95 cells; SHA pin matched) — it predates PR #89 and later cutoff-shifting fixes (#29, #33, PR #148). With the dataset now replaced, the whole reference set must be re-established from scratch: review the new-data headline and pin with `--update-reference` once the new setup is accepted (fix #47 first so the re-pin itself is trustworthy).
- [ ] **#47 — Reproducibility gate fails open (deferred; fix before re-pinning).** `run_reproducibility.py:89-99` ignores `main()`'s None-on-failure and reads whatever stale artifacts sit in `output/_repro/` → spurious PASS, or stale numbers frozen via `--update-reference`. Also: `-s` injects the segment *name* as `segment_filter` (`:43-51`, segments.toml never consulted — `-s known` filters on literal `"known"`), and the recorded `config_hash`/`git_commit` are never compared despite the docstring (`reproducibility.py:107-186`). Fix: raise on pipeline failure, clean the run dir, resolve the filter from segments.toml, compare the config hash. The code bug is dataset-independent, but there is no point exercising the gate until new references exist — fold the fix into the re-validation pass.

### P1 — Governance gates that fail open (validation evidence can silently lie)

- [ ] **#40 (resimulate model-glob done PR #162; metadata/edge-validation halves remain).** ✅ The `--resimulate` model-glob half — `run_resimulation` now resolves THIS segment's `modelling_supersegment` (via `_resolve_resimulation_model_path`) and uses `output/_supersegment_<ms>/models/` instead of a lexicographic glob over ALL `_supersegment_*` dirs (which picked the wrong model once a 2nd modelling supersegment existed); last-resort glob kept but loud. **Remaining:** (a) `persistence.py:328-361` — bin edges/multiplier/variables not persisted in model metadata nor validated at load (`--reuse-models` globs the newest dir; `metadata.json` outside the SHA sidecar); (b) the staleness hash covers only `variables|multiplier|data_path` and only warns (`main.py`); (c) supersegment model trains on globally-learned edges while members apply reporting-supersegment edges (`run_batch.py`). Fix: persist + validate edges/multiplier/variables in metadata, fail loudly on mismatch, validate edge consistency at batch start.
- [ ] **#63 — Config typos silently ignored.** No `extra="forbid"` on `PreprocessingSettings` (`config.py:243-246`) — a misspelled governance-relevant key (`mr_reoptimize_cutoffs`, `dq_allow_warnings`, …) falls back to its default with no warning. Fix: `extra="forbid"` (or a loud unknown-key warning for back-compat).

### P2 — Statistically biased numbers (mostly risk-downward / confidence-overstating)

- [ ] **#52 — MR maturity filter is leaky (part (a) done PR #151; (b)/(c) remain).** ✅ (a) day-of-month arithmetic fixed in `_compute_account_maturity` (+ removed the duplicated inline `(year,month)` diff), matching the M4 backtest's `DateOffset`. **Remaining:** (b) summary-table H3 aggregates and the H6≥H3 floor are never maturity-filtered — only `_compute_hybrid_mr_risk` filters (`:1825,78-94,239-248`); invasive + monitoring-only (M3a), deferred. (c) maturity reference = configured `date_fin_book_obs_mr` not the data snapshot (`:1790`) — a window end past the data end overstates maturity; anchor on `min(window end, max(mis_date))` (needs the data max at the call site) — deferred, monitoring-only.
- [x] **#53 — dual score+non-score reject-reason precedence (part 1 PR #151; part 2 PR #161).** ✅ Part 1 (the `update_oa_amt_h0` ordering) — status relabel runs before the H0 update so direct-measure rejections carry the demanded amount. ✅ Part 2 (PR #161, **credit-team-confirmed: non-score KO wins**) — a row that tripped BOTH a score measure and a non-score direct measure (fraud/legal/policy KO) now stays `08-other` instead of being overwritten to `09-score`. Only score-ONLY rejections (`score_measure_mask & ~non_score_mask`, where non-score = `m_ct_direct*` cols not in `score_measures`) get `09-score` / swap-in eligibility, so a dual-measure loan is no longer counted as swap-in production it could never book or fed into reject inference. Logs the dual-measure count. **Decision-relevant** (changes the swap-in/repesca population → cutoffs) — validate real-data before/after on the retrained pooled model. +2 tests.
- [ ] **#38 — Scenario risk target quantized by banker's `round(x, 1)` (report-only #55 half done PR #156).** ✅ (PR #156) the report-only sensitivity fixes: `perturb_risk_summary` now perturbs the TOTAL risk (was only `_rep` → booked-driven cells never flipped, robustness overstated); `run_sensitivity_phase` reuses the base scenario's **shipped** `acceptance_mask` (from `optimal_solution_base.csv`) instead of re-solving at the unrounded target (so the baseline matches the deployed policy under `ci_upper`/`fixed_cutoffs`); and sensitivity now runs AFTER the RI optimizer (step 6e, was 6b). **Remaining (decision-relevant):** `scenarios.py:88` `current_risk = float(round(scenario_risk, 1))` quantizes the SELECTION target with banker's rounding — `optimum_risk=1.25` selects at ≤1.2%, and a sub-0.1 `risk_step` can collapse pessimistic/base to one target. Fix: round for display only, use the exact configured value for selection. **Changes shipped cutoffs when the configured target/step isn't 1-decimal** — validate real-data before/after (no-op on 1-decimal configs).
### P3 — Policy-application consistency (same policy, different behavior by code path)

- [ ] **#57 — The two policy encodings disagree on phantom bins.** `optimization_utils.py:1055-1071` (`mask_to_cutoffs`) collapses each row to its max accepted bin, so phantom bins inside the accepted range are accepted by every cut_map consumer (`CutoffSpec.classify`, audit/MR/bootstrap 2-var paths) but rejected by `classify_by_mask` — same policy, two behaviors on new cohorts. Fix: make the mask the single source of truth (or fail when the mask isn't representable as cutoffs).
- [ ] **#39 — Three inconsistent segment-matching semantics** (2nd pass; re-verified 3rd pass). Exact `==` for single segments; prefix-anchored `str.match` in supersegment training + DQ (`preprocess_improved.py:142`, `data_quality.py:190,230,390` — a future `premium_plus` gets absorbed into shared-model training); substring `str.contains` in supersegment bin learning (`run_batch.py:249` — "premium" matches `no_premium_*`, dormant only because current supersegments supply fixed edges). Fix: `fullmatch`/`isin` everywhere.
- [ ] **#60 — Learned bins silently clobber per-segment fixed `bin_edges`.** `run_batch.py:425-439`: global/supersegment edge injection runs unconditionally AFTER `merge_configs` — a segment pinning explicit edges in segments.toml runs on a different grid than configured, no distinguishing log. Fix: skip injection when the segment config pins edges explicitly; log which source won.

### P4 — Management-facing display & aggregation

- [x] **#50 — Interactive allocator: 100× risk display (PR #152) + dead "Locked" dropdown wired through (PR #160).** The dropdown was dead because `locked_sol_fac` was never populated, so the `locked=="Yes" and locked_sol_fac is not None` guard was always False. Now `run_optimization` writes each segment's chosen `sol_fac` back into the constraints table (new read-only `Locked → sol_fac` column, `allow_duplicate=True` output), so a `Locked = Yes` segment is pinned to its last-run frontier point on the next run — matching the CLI `run_allocation.py --lock`. Constraint-building + writeback extracted into pure helpers (`_build_segment_constraints`, `_apply_allocation_to_table`) and tested (incl. the full roundtrip: lock inert on run 1, pinned on run 2). +7 tests.
- [ ] **#49 (booked-before proxy + dropna remain) — Risk surfaces.** ✅ (PR #152) `classify_cells` now raises on a missing required risk column (`todu_30ever_h6`/`todu_amt_pile_h6`) — was silent `np.zeros` → fabricated flat-0/empty surface on the deck; the 3 callers skip that surface with a visible warning, and a missing booked-before column warns. **Remaining (design-confirmation):** the "booked before" proxy (`todu_amt_pile_h6_boo > 0`) miscolours booked-but-immature cells as swap-in — switching to `oa_amt_boo` touches the Inigo-confirmed booked-before definition, so confirm first. Also `aggregate_to_cells` `groupby(dropna=True)` silently drops NaN-keyed cells (`:84-95`) — the caller already `dropna(subset=variables)`, so log the dropped exposure rather than hide it.
- [x] **#62 — Consolidation pooled rates (PR #152) + exec-summary scenario fallback (PR #162).** ✅ (PR #152) `_rejections_from_rows` None-poisons when any member lacks the rate; baseline classification grid gated off. ✅ (PR #162) `_get_total_row` and `_pick_total_row` no longer silently substitute a different scenario's TOTAL row under the requested (base) label when the base scenario is absent (legacy two-numeric-scenario runs) — they return `None` (callers degrade to "N/A") and log the gap, rather than showing e.g. pessimistic numbers as "base". +3 tests.

### LOW (brief, merged across passes)

- [x] **Pareto tie-handling** — **done in PR #180.** The three MILP/GA frontier sweeps sorted by risk only (`df["b2_ever_h6"].argsort()`), leaving the within-risk-tie order arbitrary, so the strict-`>` sweep could keep a lower-production point at equal risk (dominated); the legacy 2-var `get_optimal_solutions` used `cummax` + `>=` on a production-ASCENDING sort (kept dominated ties + equal-production-higher-risk points). All four now sort **(risk ASC, production DESC)** via `np.lexsort((-prod, risk))` and prune with the canonical `pareto_keep_indices` (strict `>`), so the paths are identical (audit #13 single source of truth). **Real-data before/after (Sep-4 batch, all 7 segments):** every frontier already had 0 risk ties + 0 dominated points, so old vs new orderings are byte-identical — the fix is a proven no-op on current data (no cutoffs move) while correcting the latent bug for when ties occur. +3 tests.
- [x] **Infeasible-threshold fallback** was labeled "Optimum selected" with only a log warning (`plots.py`) — **done in PR #172 (feature, fuller treatment).** `RiskProductionVisualizer` now records `_selection_feasible` (False only when NO Pareto point met `optimum_risk` → min-risk fallback; a locked `target_sol_fac` stays feasible) and `get_summary_table` carries it as a `Feasible` column, so the flag rides ALONGSIDE the unchanged `"Optimum selected"` label (no exact-string consumer broken) and survives the CSV round-trip. Surfaced three ways: (a) a red ⚠ banner on the segment report's risk/production section (`reporting._feasibility_banner`), (b) a prominent ⚠ line in the consolidated Exec-Summary "Recommendation & key risks" block, and (c) a dedicated "Risk-target feasibility" section on the "Validation & Governance" sheet (`consolidation._infeasible_segments`). `scenarios.py` forces `Feasible=True` for baseline/fixed-cutoff runs (target-feasibility doesn't apply to a deliberate policy). Raw column excluded from all table displays. +18 tests.
- [ ] **MILP `time_limit` hit treated as infeasible** (`optimization_utils.py`) — ~~log timeout distinctly~~ → a scipy `milp` status 1 (time limit) now logs a distinct warning (vs status 2 infeasible), so a thin frontier isn't misread as "no solution here", **done in PR #176**. Still open (decision-relevant): actually RETURNING the feasible incumbent on timeout would move the frontier → needs validation. Related: base scenario runs `process_mr_period` twice with a time-limited MILP — default-named vs `_base` MR artifacts can diverge (`scenarios.py`); dedupe or copy (deferred).
- [ ] **H3→H6 curvature** fitted cross-bin, applied within-bin temporally (`utils.py:227-322`, `mr_pipeline.py:464-486`) — ecological transfer; document/monitor, validate alpha on within-bin temporal pairs when ≥2 periods exist.
- [x] **Reject-multiplier isotonic projections unweighted** by bin evidence — **done in PR #186.** Both the axis-wise `IsotonicRegression.fit_transform` calls and the #17 poset block-pooling PAVA (`_fix_partial_order_violations`) pooled adjacent violators by the **unweighted** mean, so a 5-loan bin counted 1:1 with a 50k-loan bin when they merged — the thin bin's noisy multiplier dragged the pooled value as much as the well-observed one. Now exposure-weighted: `apply_parceling_adjustment` builds a per-cell `_ri_evidence = (n_booked + n_score_rejected).clip(min=1)` column, threaded via a new `weight_col` param into `_enforce_multiplier_monotonicity` (passed as `sample_weight=` to both isotonic fits, via `_slice_weights` which falls back to unweighted when a slice's weights are non-finite / sum ~0) and into `_fix_partial_order_violations` (block value = `Σ(w·mult)/Σw`, weights floored at 1e-9 against a singleton zero-denominator); the internal column is dropped before return. **Real-data before/after (Sep-4 standalone, the two segments that can bite):** on `direct-pl-known-premium-precon` (varied multipliers 3.903/4.114 **and** active swap-in €556k) the multipliers demonstrably shift (avg 3.903→3.768, max 4.114→4.196; poset pooling 37→148 blocks) yet the selected optimum is **byte-identical** — 208 accepted cells (0 added/0 removed), risk 1.3947%, prod €118.48M, swap-in €555,989 @14.50%, swap-out €1.87M all unchanged to 6 dp; on `direct-conso-known-premium` (varied 1.887/1.944 but accept-all optimum) likewise 320→320 identical. The remaining 5 segments have uniform/capped multipliers (weighted mean of equal values == that value → PAVA has nothing to pool → provably no-op). So the multiplier reshuffling never crosses a decision boundary: correctness/robustness fix with **zero cutoff movement** on current data. +2 tests (weighted isotonic + weighted poset pool toward the high-evidence bin).
- [x] **RI internals — fully closed (PRs #170 & #181).** ~~asymmetric α/β floors shift the Beta prior mean anti-conservatively~~ → **unbiased Beta prior** in PR #181: `alpha=strength·gr`, `beta=strength·(1-gr)` (mean == `global_rate` by construction, `gr` clipped off exact 0/1), replacing the per-component `max(·,0.5)` floors that pinned the prior mean near 0.5 when `global_rate` was near 0/1 — e.g. a deep-reject `global_rate=0.016` had its prior mean inflated to 0.025 (+55%), over-stating inferred acceptance in the riskiest bins. ~~`max(Σw,1)` denominator wrong under decay weights~~ → zero-guarded `total.sum()` (the clamp-to-1 understated `global_rate` when the decayed effective weight `Σw<1`), PR #181. **Real-data before/after (Sep-4 batch):** only `direct-pl-new` hit the floor (7 low-acceptance contexts; prior mean corrected up to −35%), yet the selected optimum is **byte-identical** (risk 1.1837%, prod €8.57M, 32 accepted cells, 659 frontier points) — the corrected deep-reject bins were already rejected at the 1.18% optimum, and the other 6 segments never trigger the floor. So the anti-conservative RI bias is fixed with **zero cutoff movement** on current data. +1 test. (Earlier in #170: decay NameError guard + tie-break docstring.)
- [ ] **MR monitoring:** PSI on booked-only populations (`:1380+` — standard basis is demand). ~~`mr_observed` label assigned without requiring non-NaN `b2_mr`~~ → **done in PR #185**: `use_mr` now requires `n_obs_mr >= min_obs` AND `b2_mr.notna()`, so a bin that clears the obs threshold but has an undefined MR rate (zero H6 exposure/pile) is no longer labeled `mr_observed` with a NaN risk — it falls through to H3-extrapolation / main-imputation / model-fallback. **Real-data before/after (Sep-4 batch): no-op** — every segment logged `mr_observed=0` (MR cohort too immature for matured H6, so `use_mr` is already all-False; narrowing an all-False mask can't change the surface). Correctness guard for when the MR cohort matures. +1 test. ~~summary H3-floor clamps displayed risk but not persisted numerators~~ → clamp now also rewrites `todu_30ever_h6` so `multiplier·num/den` reproduces the shown floored rate, **done in PR #167**. ~~fallback stability vars from an unsorted `set` → non-reproducible PSI~~ and ~~empty-population PSI returns 0.0 → STABLE~~ **done in PR #163**.
- [x] **Monthly `risk_rate` includes H6-immature months** (`trends.py`) — **done in PR #164.** `compute_monthly_metrics` takes an opt-in `maturity_months`; `risk_rate` is blanked (NaN) for booking months within `maturity_months` of the latest observed month (reference = `max(year_month)`, the same latest-observation proxy the MR maturity filter uses), killing the spurious falling tail. Production/approval are H0 and untouched. main.py threads `settings.mr_maturity_months`; default `None` = backward-compatible.
- [ ] **Backtest:** ~~drift flag one-sided — OOT CI entirely *below* in-sample reads "OK"~~ (now returns **BELOW**), ~~loan-level iid bootstrap ignores vintage clustering~~ (limitation now stated in the docstring), and ~~per-cell calibration TypeErrors on object-dtype None~~ (→ `np.nan`) — all **done in PR #163**.
- [x] **`compute_ri_gini` imputes bad-rate 0 for booked-empty deciles** (`selection_bias.py`) — **done in PR #164.** A fully-rejected band (no booked loans → no observed bad rate) used to keep the `np.zeros` default → every rejected loan there imputed GOOD, anti-conservative exactly in the riskiest deepest-reject tail. New module-level `_nearest_decile_rate` inherits the nearest covered decile's rate (ties → the higher rate, the conservative side) before applying the uplift; the loop now iterates the deciles actually present among rejects. Diagnostic-only.
- [x] **Metrics:** **fully done across PRs #163/#164/#166.** ~~degenerate bootstrap resamples silently skipped~~ → both bootstrap fns now log how many resamples were single-class/dropped (`_warn_if_many_bootstrap_skips`, warn >20%); ~~discriminance rows label NaN-masked metrics with full-population counts~~ → `compute_score_discriminance` counts `n_records`/`n_bads`/`bad_rate` on the valid (non-NaN-score) subset the metric was actually computed on; ~~combined-score OOB bootstrap CI systematically pessimistic vs its OOF point estimate~~ → documented in `_bootstrap_ci_combined` as a deliberately-conservative lower-leaning band (replicates train on ~63% + score on OOB vs the K-fold OOF point estimate), not a symmetric bracket — **all PR #166**. (Earlier: `(0.0,0.0)` sentinel → NaN in #163; `calculate_psi_by_period` empty-window/NaN-deflation in #164; "inclusive boundaries double-count" moot on the histogram path.)
- [ ] **Config/preprocessing:** MAD-zero fallback keeps the 0.6745 constant with mean-AD (`data_quality.py:314-321`) — the correct MeanAD consistency constant is ~0.7979, but this **changes which rows the DQ gate flags** (the current data already trips the outlier check → needs `--allow-dq-warnings`), so it's decision-adjacent, not report-only. ~~`calculate_annual_coef` mis-annualizes non-aligned windows~~ → **done in PR #184**: on inspection `n_month` counts whole monthly booking COHORTS (inclusive month labels), which is the CORRECT annualization basis for cohort-aggregated production — switching to elapsed days would be wrong and would inflate the current (aligned) production ~+9%. The real gap was that a NON-month-aligned window (mid-month / sub-month endpoints) silently got a wrong cohort count; now `calculate_annual_coef` WARNS when an endpoint's `day != 1` and the docstring documents the cohort basis. Transparency only — the coefficient is unchanged for aligned windows (the live config dates are all day-1, so no warning fires and no number moves). ~~`variables` validator allows 1 vs documented ≥2~~ — **NOT a bug**: single-variable (1-D) configs are intentionally supported now (the CLAUDE.md "≥2" wording is the stale side; the validator + `test_variables_wrong_length` already allow 1). ~~MR-overlap validator strict `<`~~ → `<=` so a shared inclusive-boundary day also warns, and ~~`optimize_dtypes` wraps values > uint32 max~~ → out-of-32-bit-range ints now kept `int64` (no silent wrap) — both **done in PR #167**.
- [x] **Binning edges — quantile honest-warnings (PR #183).** ~~quantile-tie bin collapse only INFO-logged~~ → `learn_quantile_bins` now WARNS when ties collapse `max_bins-1` requested splits to fewer (a silently-coarsened grid). ~~right-closed cut skews "equal-count" on discrete sources~~ → kept the **value-based** cut on purpose (clean, reproducible, implementable cutpoint — rank-splitting a mass point would treat identical incomes differently and isn't reproducible; Inigo-confirmed), made `right=True` explicit + documented, fixed the misleading "equal counts" docstring, and added a WARNING in `_apply_binning_from_config` when a quantile binning's realized populations are materially unequal (max/min > 1.5). **No edges/cutoffs change** — transparency only (verified: e.g. `income_bin`'s live 62/38 skew is now surfaced, not silently presented as equal-count). +3 tests. ~~out-of-range bin gate conflates NaN-source rows / drops silently~~ — **already fixed** (audit #21: below/above/NaN-source separated, out-of-range snapped to boundary + kept, only NaN-source dropped with a log); the todo was stale on that.
- [x] **Estimators/persistence — fully closed (PRs #168 & #175).** ~~tree-winner path stores CV R²=0.0 as if real~~ → NaN ("not computed") in PR #175: tree models are tuned/selected on RMSE only, so `tune_*_models` emit no "CV Mean R2" column and a tree-winner's `best_row.get("CV Mean R2", 0.0)` recorded **0.0** — indistinguishable from a genuinely flat model (the #65 case). Now defaults to `float("nan")` (tree-winner + feature-set paths), and `_select_feature_set_cv` records NaN for an undefined R² (no scored folds / single-fold std). Metadata-only (no consumer of `cv_mean_r2`/`cv_std_r2` outside inference/persistence; NaN is format-safe + already tolerated in metadata.json). ~~`HurdleRegressor.set_params` silently drops nested params~~ → now RAISES (sklearn semantics) when a nested param targets a `None` sub-estimator, and applies top-level before nested; ~~bare AttributeError instead of NotFittedError~~ → `predict`/`predict_binary`/`predict_magnitude` call `check_is_fitted` (→ `NotFittedError`); ~~`predict_on_new_data` crashes on single-class hurdle classifier~~ → the diagnostic block is shape-guarded + wrapped so a 1-column `predict_proba` no longer aborts a valid prediction; ~~same-second model saves silently overwrite~~ → colliding `model_<ts>` dirs get a counter suffix (no lost model) — **all done in PR #168**. (`get_params(deep)` is already complete — returns all three constructor params + nested — so no change needed.)
- [ ] **Optimization edges:** exposure-as-binomial-n SE makes the (default-off) monotonicity relaxation statistically inert (`:370-373`) — decision-relevant, default-off. ~~GA fallback drops all H3 KPI columns~~ → `_ga_pareto_fallback` now threads `multiplier_h3` into its `evaluate_solution` call (the MILP path already did), so GA-fallback solutions carry `b2_ever_h3` too (report-completeness; H3 is not used for Pareto selection, so no cutoff moves), **done in PR #176**. ~~configured swap-in caps silently skipped when `_rep` columns absent~~ → `trace_pareto_frontier` now warns ONCE (`_warn_unenforceable_swapin_caps`) when a cap is set but the `_rep` columns are absent (covers the MILP + GA paths; log-only), **done in PR #174**. ~~`run_allocation` double-counts when both directory layouts exist~~ → `_discover_segment_frontiers` prefers the multi-segment (batch) layout and only falls back to single, so a root with both layouts no longer counts itself as an extra segment, **done in PR #174**. ~~lineage `git rev-parse` runs without `cwd=`~~ → **already done in PR #163**.
- [x] **Report display — fully closed across PRs #169 & #173.** ~~`plot_3d_graph` unweighted-mean marginalization~~ → **exposure-weighted** (`_weighted_group_mean`, weight = first of `todu_amt_pile_h6`/`oa_amt_h0`/`oa_amt`; unweighted fallback + a zero-weight-group plain-mean fallback), PR #173. ~~"Production (%)" fraction-vs-percent-vs-delta~~ → stored as a **true percent** (×100) in all three producers (`plots`/`audit`/`mr_pipeline`), consistent with the "0.00" number format and every other "(%)" column; `production_delta_pct` (consolidated) unaffected, PR #173. ~~mixed-grid consolidated matrices silently vanish~~ → `_build_acceptance_matrices` now derives grid vars **per segment** (a 2-var segment no longer drops out under a NaN slice group) + `dropna=False` + logs the >12-slice cap, PR #173. ~~`plot_3d_surface` clips bin 0~~ → axis range starts at the actual `x_min`/`y_min`, not a hard-coded 1, PR #173. (Earlier in #169: ≥4-scenario collapse warning, three-state 1D acceptance rate + NaN-safety, truncation logging.)

### Adjudicated / no action

- **Production-weighted allocation risk** — re-flagged by both the 2nd and 3rd passes (incl. `portfolio_owner.py:216-225` enforcing the committee target on the production-weighted figure); adjudicated as documented-intentional in closed **#14** (exposure-weighted rate reported alongside; 0.0008pp gap on the live portfolio). No re-open unless the business convention changes.
- **Phantom "diagonal wall" monotonicity relaxation** — re-derived by the 3rd pass; adjudicated as the known fail-safe relaxation in closed **#22** (no force-reject; no data sits between the unconstrained pair).
- Verified clean (2nd pass): exposure-weighted risk aggregation throughout the optimization layer; adjacent-pair monotonicity composes to full coordinatewise monotonicity; bins learned on date-filtered demand, no main→MR look-ahead; no shared-DataFrame mutation in batch/parallel; per-loan hurdle scored apples-to-apples; SPC limits exclude the judged point; lineage hashes contents; backtest windows disjoint with fail-closed bin matching; pooled consolidation risk sums numerators/denominators; supervised binning dead outside tests.
- Verified clean (3rd pass): parceling math (uplift on H6 numerator, score-rejected base, no double-counting); `1−exp(−n/scale)` anchor shrinkage; Beta-Binomial posterior + EB moment estimator; poset PAVA; MILP risk-ratio linearization + direction conventions; floor/ceiling pin directions; global-allocation MILP decode; H3 curvature-fit math (weighted log-log, SE, CI-includes-1 rule); backtest window edges + bootstrap resampler; paired-bootstrap resampling unit + verdict thresholds; DeLong covariance; Nadeau–Bengio SE; pooled exposure-weighted risk in `aggregate_metrics`; the documented system-KO rule.

---

## Notes

- Future feature (deferred until the errors above are fixed): **hierarchical reporting supersegments / nested subtotals** for the new channel hierarchy — spec in `new_feature_specs.md`; fixing #45 and #62 first (or with it) is recommended since it builds on the same code.
- Suggested order: **done so far — #150 (#48+#61); #151 (#51/#54/#52a/#53p1); #152 (#50 100×/#62/#49 core); #153 (#58); #154 (#41); #155 (#59); #156 (#38/#55 report-only); #157 (#45+#64); #158 (#56 winsorize); #159 (#65 constant-model); #160 (#50 lock).** Next (safe): the remaining leftover sub-parts (#49 booked-before proxy — needs design confirmation; #62 exec-summary scenario fallback) + #40's `--resimulate` model-glob half, and the grouped LOW list. Decision-relevant / needs real data (hold for the new dataset): #38 item 1 (target rounding), #52(b)/(c). Also validate #56/#58/#65 real-data before/after once the pooled model is retrained. The reproducibility items (#47 + the M5 re-pin) stay deferred until the new dataset/product setup stabilizes.
- Confidence: the numbered P1–P4 items were directly re-verified in source on 2026-08-12; the LOW bullets are careful agent reviews with quoted code at medium-high confidence. History shows several original HIGHs were overstated on close reading (#2, #3, #11, #13) — confirm each before changing behavior, and validate anything cutoff-touching on real data (the Done entries below document the before/after discipline to follow).

---

# Done / History

## Production-Hardening Milestone

**Goal:** raise the codebase from "production-grade software" to "production-grade credit-decisioning system" — i.e. trustworthy enough to drive live cutoffs and survive independent model validation. The engineering hygiene is already strong (≈1,130 tests, ruff, CI, typed/validated config, MR + PSI/CSI + bootstrap diagnostics). The gap is methodological correctness in decision-critical paths and decisioning governance: bugs here don't crash, they silently bias the risk/production numbers that set lending cutoffs.

Exit criteria:

- [x] **M1 — Clear decision-critical HIGH findings.** Resolve the open Tier-1 items that touch numbers feeding cutoffs, model training/selection (#6, #7, #8 done) and the RI/aggregation logic (#4, #5 done) — all cleared. Each fix verified on real data with the before/after delta reported.
  - **Done:** every referenced Tier-1 finding (#4, #5, #6, #7, #8) is implemented, real-data-verified on `no_premium_cd`, and merged; deltas recorded in their `## Done` entries below. Combined with the full findings sweep (#1–#21 + the 3 sub-findings), the decision-critical correctness backlog is cleared.
  - **Carve-out (does not block M1, tracked under M5):** #7 changes the *selected risk model* (−0.42% aggregate). The finding is **resolved** (bug fixed + verified), but flipping it to drive **automatic live cutoffs** still needs the second-segment validation / MRM sign-off — that deployment gate lives in M5, not here.
- [x] **M2 — Data quality becomes a hard gate (#18).** Date-coverage gaps/parse failures, negative counts/amounts, and per-segment booked-ratio checks FAIL the run (not warn) by default; add a documented `--allow-dq-warnings` escape hatch for analysts. Capture the data snapshot/extraction date + basic lineage with each run.
  - **Done (fail-closed default):** `dq_allow_warnings` default flipped `true→false` (`src/config.py`). WARNING-severity checks (coverage gaps, outliers, small segments, booked-ratio 0.01–0.05) now halt by default alongside the FAILED-severity checks that already did. `--allow-dq-warnings` CLI flag added to `main.py` + `run_batch.py` as the analyst escape hatch (relaxes only the WARNING tier; `--skip-dq-checks` still skips DQ entirely). Gate message reworded to name the flag.
  - **Done (lineage):** every run writes `output/<segment>/data/run_lineage.json` (`src/lineage.py`) — source data path/size/mtime/SHA-256, loaded row count, run-id + UTC timestamp, git commit(+dirty), config path/hash, headline assumptions. Run banner logged at entry; batch shares one canonical run-id across segments. Surfaced in the segment HTML report ("Data Lineage / Provenance") and the consolidated Excel exec summary. Best-effort (never aborts the run).
  - **Real-data check (`no_premium_cd`, 1.44M rows):** the segment carries exactly 1 WARNING (numeric outliers, e.g. oa_amt 9.13% z>5) → it now **halts by default** (previously proceeded) and runs clean with `--allow-dq-warnings`. Optimum is **byte-identical** to the pre-change baseline (same model reused: risk 1.0858%, production €40.45M, 96 accepted cells) — the gate only halts/proceeds; lineage is read-only.
  - **Found+fixed in passing:** parallel batch mode mis-mapped `run_segment_pipeline` positional args (since `base_scenario_only` was inserted into the signature, `base_scenario_only ← global_bin_edges`, `floor_cells_path ← "floor"`, …) — converted that `executor.submit` to keyword args.
- [x] **M3 — Config-complexity audit.** Inventory which features are load-bearing vs. experimental; set safe defaults; ship a documented "simple mode" with the fragile features (RI optimizer, MR auto-extrapolation, monotonicity relaxation, optimization binning) off by default. Rationale: the live config stacks many interacting knobs on exactly the most fragile paths, and two live-config footguns (`method="optimization"`, `reject_include_all_rejections=true`) were already found and fixed.
  - Tooling: `config_sensitivity.py` runs a one-at-a-time sweep and ranks knobs by headline impact (production %, risk %, accepted-cell changes). Risk is held ~constant by the fixed risk target, so sensitivity surfaces as **production**.
  - **Full-registry result (`no_premium_cd`, 35 runs, run_ri_optimizer off, baseline €52.5M / 1.09% / 157 cells):**
    - Tier 1 — material: `stress_mode` ±29.4%, `multiplier` ±22.2%, `reject_parceling_method` ±9.9%, `reject_uplift_factor` ±8.7%.
    - Tier 2 — minor (<1%): `reject_bayesian_prior_strength`, `reject_decay_half_life`, `reject_max_risk_multiplier`, `pareto_n_points`, `per_bin_tasa_fin`.
    - Tier 3 — **zero effect** (bit-identical output): `multiplier_h3`, all six MR knobs, `monotonicity_relaxation` + `mono_z_threshold`, `ri_calibration_gamma`/`run_ri_optimizer` (last two are artifacts of run_ri_optimizer=off in the demo config).
  - **Optimizer-on partial sweep (`run_ri_optimizer=true`, ri trials reduced):** toggling the optimizer on drops baseline €52.5M→€42.5M (±19%); `ri_calibration_gamma` ±24% (now the active RI lever); `multiplier` material; `reject_parceling_method` ±5%. **Key interaction:** with the optimizer ON, `reject_uplift_factor` and `reject_max_risk_multiplier` are bit-identical inert (the optimizer overrides them). So the RI knobs are mutually exclusive by mode.
  - **Tier classification:**
    - **Core** (must set, high impact): `variables`/`inference_variables`/`bins`/`directions`, dates, `data_path`, `segment_filter`, `optimum_risk`/`risk_step`. (NB: `multiplier`/`multiplier_h3` show huge sweep impact but are FIXED metric-definition constants — 7 = H0..H6 months, 4 = H0..H3 — to *lock and validate*, not tune.)
    - **Tuning** (governed, material): `stress_mode` (±29%, biggest lever), `reject_inference_method`/`reject_parceling_method` (±10%), `run_ri_optimizer` (toggle ±19%).
    - **Tuning, mode-dependent:** optimizer **off** → `reject_uplift_factor`/`reject_max_risk_multiplier` (±9%); optimizer **on** → `ri_calibration_gamma` (±24%) + `ri_*_range`. The other set goes inert.
    - **Expert / default-off** (fragile, low/zero impact): MR suite (validation-only, 0 cutoff impact — M3a), `monotonicity_relaxation_enabled`/`mono_z_threshold` (inert), `reject_bayesian_smoothing`/prior/decay (<1%), `mr_extrapolation_*`, `multiplier_h3`, swap-in constraints.
    - **Deprecated:** `reject_include_all_rejections` (#3), `octroi_bins`/`efx_bins` (legacy), binning `method="optimization"` (#1).
  - **Defaults finding:** the Pydantic defaults in `config.py` are ALREADY the simple/safe path — every fragile feature defaults off. The only non-minimal default is `stress_mode="global"` (a deliberate risk-conservatism choice), whose one footgun (stress×parceling double-counting) already has a runtime guardrail (`pipeline/preprocessing.py:117`). So no default changes were needed; tier classification is documented inline in `config.py`. The complexity is in `config.toml` (kitchen-sink), not the code defaults.
  - **Done:** shipped `simple.toml` — a minimal Core-only starting template (2-var grid via modern `BinConfig`, all fragile features omitted → safe defaults; RI/optimizer/MR/smoothing off, stress disabled). Validated end-to-end (no_premium_cd → €55.9M / 1.088%). `config.toml` is the "advanced/full" example.
  - **Generality check (done — reports in `reports/sensitivity/`):** widened the Tier-1 grids to ~4 points (`config_sensitivity.py`) and re-swept with **per-run full Optuna training** on a second segment. Intended `conso_known` has **0 rows** in this snapshot → substituted **`premium`** (67k rows). Three sweeps: `no_premium_cd` OFF, `premium` OFF, `premium` ON (scoped RI knobs, `ri_trials=30` — a full-registry 200-trial ON sweep is ~8 h).
    - **`multiplier` dominates on both segments (cd 52%, premium 56%) but is a FIXED accounting constant, not a lever:** `todu_amt_pile_hN` sums outstanding over months H0..HN, so the `7` in `b2_ever_h6 = 7·Σ(todu_30ever_h6)/Σ(todu_amt_pile_h6)` converts that sum to the average monthly outstanding (H0..H6 = 7 months; `multiplier_h3 = 4` for H0..H3). Its large sweep impact is a **guardrail** signal (lock + validate it), not a tuning degree of freedom.
    - **The rest of the cd Tier-1 set does NOT generalize:** `stress_mode` (28%), `reject_uplift_factor` (13%), `reject_parceling_method` (11%) are material on `no_premium_cd` but **byte-identical inert on `premium`**. Root cause: premium's rejections are **95.2% non-score** (vs 59.7% on cd) → reject inference has no score-mass to parcel, and stress is ~constant across modes there. **Tuning-tier materiality is segment-dependent**, keyed on the score-rejection share — govern stress/reject **per-segment** for the no_premium/known-PL family; they're moot for premium.
    - **The Expert/default-off set generalizes (the reassuring result):** MR suite, `multiplier_h3`, `monotonicity_*`, `pareto_n_points`, `per_bin_tasa_fin` ≈ 0 on **both** segments → "defaults are safe" holds on a second segment.
    - **RI mode-exclusivity confirmed mechanically:** in premium ON the RI optimizer overrode the manual reject params (`uplift=0.80, max_mult=1.00`), so `reject_uplift`/`max_mult` are inert in ON mode by construction.
    - Widened grids **reproduce** the original cd Tier-1 ordering (`multiplier > stress_mode > reject_uplift > reject_parceling`) — robust to grid resolution. Diagnostic only; no production-default or cutoff impact.
- [x] **M3a — Confirmed: MR does not feed the optimizer (validation-only).** The Tier-3 zero-impact of every MR knob is by design, not a wiring bug. `process_mr_period` is invoked only in `scenarios.py` (`run_scenario_analysis`), *after* `run_optimization_phase` builds the Pareto frontier and *after* `opt_sol` (the chosen cutoffs) is written; it receives the already-selected cutoffs as inputs (`optimal_solution_df=opt_sol`, `mask=selected_mask`) and its return value is discarded — it only writes `mr_*` validation CSVs. Implication: the MR subsystem (and its audit findings, e.g. #2) affects **monitoring accuracy**, not live cutoffs — so those findings can be re-prioritized below anything that touches the optimizer. Action for M3: document MR explicitly as a diagnostic/validation point and default its knobs to a simple, stable config.
- [x] **M4 — Independent out-of-time backtest of cutoffs.** Backtest proposed cutoffs against a held-out period (beyond the existing MR check); compare realized vs. predicted risk/production. Make this a repeatable artifact, not a one-off.
  - **Done:** `run_backtest.py` + `src/backtest.py` apply each segment's **frozen** accepted-cell set (`accepted_cells*.csv`) **as-is** to an auto-derived held-out cohort and compare realized vs predicted risk/production. Distinct from the inline MR check (which re-optimizes the mask + is risk-only); fully read-only (writes only under `--output`). Held-out window = `(date_fin_book_obs, max(mis_date) − maturity_months]` (post-training cohorts old enough for a realized H6 — no extrapolation). Production framed as rates (acceptance %, realized €); the clean drift signal is **in-sample realized → OOT realized** (same booked-only basis) — `predicted` carries RI+stress conservatism. Standalone single-segment + batch + consolidated CSV/markdown; per-cell calibration with booked counts. Tests in `tests/test_backtest.py` (10).
  - **Real-data finding (5 frozen segments, base scenario):** out-of-time risk drift is **concentrated in the no_premium family** — no_premium_cd 0.97%→**2.11%** (+1.14pp), no_premium_ef 1.08%→**2.62%** (+1.54pp) flagged DRIFT; no_premium_ab (+0.13pp), precon (−0.26pp), premium (+0.22pp) OK. Acceptance rates stable. For premium/precon `predicted == in-sample realized` (RI inert there — ties to M3); for no_premium, predicted > realized (RI uplift). **Caveat:** small mature windows (~21–27% mature, 2 cohorts, 212–1149 booked) → a signal to investigate (esp. no_premium_cd/ef risk drift), not conclusive. Feeds M5.
- [x] **M5 — Validation & reproducibility trail.** Someone who didn't write the pipeline attempts to reproduce and break the headline numbers; document assumptions (multiplier, gamma, maturity anchor, stress mode), pin the data snapshot, and record a model-risk-management style sign-off. Required before the tool stands in front of a regulator or drives cutoffs automatically.
  - **Done:** validation pack in `reports/validation/` — `assumptions_register.md` (every assumption + value/source/governance tier; `multiplier`/`multiplier_h3` FIXED), `reproduction_runbook.md`, `model_validation_report.md` (independent reproduction + adversarial challenge), `mrm_signoff.md` (sign-off + residual risks + conditions for live/automated cutoffs), `reference/no_premium_cd_headline.json` (committed golden numbers + the data SHA-256/git/config they pin to). New `run_reproducibility.py` + `src/reproducibility.py` re-derive a segment's headline and PASS only if it reproduces within tolerance **and** the data SHA-256 still matches (a changed snapshot fails loudly → re-validate). Tests in `tests/test_reproducibility.py` (11).
  - **Reproduction (real data):** `no_premium_cd` reproduces **byte-identically** (risk 1.085788%, production €40,685,570, 96 cells; Δ 0.0pp / 0.0%; SHA-256 match) → PASS, deterministic.
  - **Challenge findings (honest):** bootstrap CI on the optimum risk is **wide** ([0.42%, 1.24%] around 1.086%); PSI drift main↔MR stable (4 info / 0 warn / 0 crit); audit reconciles with the summary KPIs. The M4 out-of-time backtest's apparent risk drift on the no_premium family was **investigated (2026-06) and found to be within sampling noise** — every OOT bootstrap CI overlaps its in-sample CI, **no segment flags DRIFT** (noise-aware rule), tiny counts (cd 12 defaults / ef 2), and **not a maturity artifact** (`pile_h6/pile_h3` ≈ 1.68 in/out of sample); cd borderline (Poisson p=0.057). The M4 backtest now reports CIs + default counts + a noise-aware flag. #7 recorded-validated (2026-05-31 multi-segment pooled). Verdict: approved for **human-in-the-loop analyst decision-support**; automated live cutoffs gated on the sign-off conditions. Diagnostic/docs only — no pipeline/default/cutoff change.

## Fixed — third pass (2026-08-12)

Seven parallel subsystem audits; every HIGH and key MEDIUM re-verified in source before listing. Fixed same-day:

- [x] **LOW batch — MILP timeout logging + GA-fallback H3 KPIs (PR #176).** Two fixes, no cutoff numbers moved. (1) `milp_solve_cutoffs`: scipy `milp` reports status 1 (iteration/time-limit reached) and status 2 (infeasible) both as `not result.success`, so a TIMEOUT was silently dropped exactly like a genuine infeasibility — thinning the Pareto frontier with no signal. Now a status-1 timeout logs a distinct warning ("hit the Ns time limit … frontier point dropped; raise milp_time_limit"); behaviour unchanged (still returns None — returning the feasible incumbent would move the frontier and needs validation). (2) `_ga_pareto_fallback` now threads `multiplier_h3` into its `evaluate_solution` call (the MILP path already passed it), so GA-fallback solutions carry the `b2_ever_h3` KPI columns instead of dropping them — the live config's `indicators` include the H3 raw columns, so this is real report-completeness; H3 is not used for Pareto selection so no cutoff moves. +4 tests (timeout logged vs infeasible not; GA carries/omits H3 with/without multiplier_h3). (Deferred: incumbent-on-timeout recovery + the scenarios.py MR-artifact dedupe are decision-relevant.)
- [x] **Tree-winner CV-R²=0.0 → NaN metadata honesty (PR #175).** When a tree model wins model selection its CV R² is never computed — `tune_tree_models`/`tune_linear_models` project to `["Model", "CV Mean RMSE", "CV Std RMSE", "model_template"]` only, so the combined results have no "CV Mean R2" column and the tree-winner branch's `best_row.get("CV Mean R2", 0.0)` recorded **0.0**, indistinguishable from a genuinely flat R²≈0 model (exactly the #65 degenerate-model symptom). Now defaults to `float("nan")` on both the tree-winner and feature-set paths, and `_select_feature_set_cv` records NaN for an undefined R² (no scored folds, or a single fold where the std is undefined) instead of 0.0. Purely metadata/display: there are **no consumers** of `cv_mean_r2`/`cv_std_r2` outside `inference_optimized`/`persistence`, model selection is RMSE-driven (1SE rule), and the #65 gate keys on prediction range — so no numbers or cutoffs move; NaN is format-string-safe (`:.4f` → "nan") and already tolerated in metadata.json (`allow_nan` + the existing `np.isnan` display guard). +1 test (monkeypatched tree winner asserts NaN, not 0.0). Closes the Estimators/persistence bullet (with #168).
- [x] **LOW batch — silent-skip / double-count guards (PR #174).** Two fixes, no cutoff numbers moved on the default path. (1) `optimization_utils.trace_pareto_frontier` warns ONCE (`_warn_unenforceable_swapin_caps`) when `max_swapin_production_pct`/`max_swapin_risk` is configured but the repesca `_rep` columns needed to enforce it are absent from the grid — the MILP/GA constraint was otherwise silently skipped per solve and the analyst believed a cap applied that never bound anything. Placed at the sweep orchestrator (once per scenario, not per risk-step) so it covers both the MILP and GA-fallback paths without log spam. (2) `run_allocation._discover_segment_frontiers` replaces the two unconditional load blocks (single-segment layout + multi-segment layout) that BOTH fired when both were present, summing the root frontier on top of the real segments; now the multi-segment (batch) layout is preferred and single-segment is a fallback only, so a root with both layouts no longer double-counts itself as an extra segment (+ deterministic `sorted` iteration). +9 tests. (Deferred: GA H3 KPI drop needs `evaluate_solution` to compute H3; the monotonicity-relaxation SE is decision-relevant + default-off.)
- [x] **Report-display viz cluster — 3D surfaces + Production-% semantics (PR #173).** Four report-only fixes, closing the Report-display bullet. (1) `plot_3d_graph`: N>2-var marginalization is now **exposure-weighted** (`_weighted_group_mean`) instead of a plain mean that let a tiny high-risk cell count as much as a huge low-risk one — weight = first present of `todu_amt_pile_h6`/`oa_amt_h0`/`oa_amt`, unweighted fallback if none, and a zero-weight group falls back to a plain mean (cell stays visible). (2) `plot_3d_surface`: 3D axis range starts at the real `x_min`/`y_min` instead of a hard-coded `1`, so **bin 0** is no longer cropped from view. (3) `_build_acceptance_matrices`: derives grid vars **per segment**, so in a consolidated view a lower-dim segment (e.g. 2-var next to 3-var) renders its own grid instead of silently vanishing when the global slice var is NaN; `dropna=False` on the slice groupby + a log when >12 slices are capped. (4) `"Production (%)"` is now a **true percent** (×100) in all three summary producers (`plots`/`audit`/`mr_pipeline`) — matching its label, the Excel `"0.00"` format, and every other `"(%)"` column (it was the lone fraction, so `1.05` read as `1.05%` not `105%`); the consolidated `production_delta_pct` is computed separately and is unaffected, and no consumer re-scales it. Display-only; no € / risk / cutoff numbers moved. +7 tests.
- [x] **FEATURE — infeasible-risk-target `Feasible` flag, fuller treatment (PR #172).** When no Pareto point meets `optimum_risk` the optimizer falls back to the min-risk solution but still labels the row `"Optimum selected"` — the target-miss was buried in a log line. Mirroring the allocation `feasible` flag (#15/#42): `RiskProductionVisualizer` now records `_selection_feasible` and `get_summary_table` carries a `Feasible` column that rides ALONGSIDE the unchanged label (no exact-string consumer in `audit`/`consolidation`/`scenarios` breaks) and persists through the CSV round-trip. Surfaced three ways — a red ⚠ banner on the segment report's risk/production section, a ⚠ line in the consolidated Exec-Summary "Recommendation & key risks" block, and a dedicated "Risk-target feasibility" section on the "Validation & Governance" sheet. Baseline/fixed-cutoff runs force `Feasible=True` (a deliberate policy isn't a target-optimization). Raw column excluded from every table display; missing column (old runs) reads as feasible. +18 tests; no numbers moved.
- [x] **LOW batch — RI decay crash-guard + tie-break docstring drift (PR #170).** Two fixes, no numbers moved on the default config. (1) `reject_inference.compute_acceptance_rates`: when time-decay weighting is requested but the date column is entirely unparseable (`max_date` is NaT), the code set `decay_half_life_months=None` but never fell through to the counts-based `else` branch, leaving `n_booked`/`n_rejected` undefined → the merge **NameErrored**. The fallback now computes the unweighted count aggregates in place (crash guard; only reachable when decay is on AND all dates are unparseable). (2) `run_reject_inference_optimization`'s docstring said ties are broken by "max oa_amt_h0" — stale, contradicting the actual `_select_best` behaviour + the module docstring (audit #29 flipped it to CONSERVATIVE/LOWEST production). Corrected the docstring; no code change. +1 test. (Deferred, decision-relevant: the α/β prior floors and the `max(Σw,1)` decay denominator move RI numbers → need real-data validation.)
- [x] **LOW batch — report-display three-state acceptance + no-silent-truncation (PR #169).** Four report-only fixes. (1) The HTML 1D acceptance strip (`reporting._build_acceptance_strip_1d`) used raw `accepted.sum()` → `int(NaN)` **crashed** on an unobserved (phantom) cell, and rendered N/A as a rejection — now NaN-safe (pandas `.sum()`), renders N/A as the three-state `m-na`, and rates over OBSERVED cells. (2) The Excel 1D strip (`consolidation._write_acceptance_strip_1d`) now rates `accepted/observed` (N/A excluded from the denominator; was `accepted/total`, counting phantom cells as rejections) + div-by-zero guard. (3) Both `_build_cutoff_reference_table` truncation sites (the N≥3-var listing + the generic `csv_to_html_table`) now log the row drop (no silent display cap). (4) `map_scenario_names` warns which legacy middle scenarios collapse to `base` when >3 numeric suffixes are present (they overwrite each other). +3 tests. Deferred (report-only but bigger/viz): `plot_3d_graph` unweighted-mean marginalization, "Production (%)" cross-file label meanings, mixed-grid consolidated matrices, `plot_3d_surface` bin-0 axis clip.
- [x] **LOW batch — estimators/persistence robustness (PR #168).** Four fixes (no cutoff impact on the current default config — `HurdleRegressor` is Expert/default-off). (1) `persistence.save_model_with_metadata`: two saves in the same wall-clock second (fast batch) used to reuse the `model_<ts>` dir under `mkdir(exist_ok=True)` and silently overwrite the first `.pkl` — now colliding dirs get a `_<n>` counter suffix (+ warning), so no model is lost. (2) `persistence.predict_on_new_data`: the hurdle diagnostic block did `classifier_.predict_proba(...)[:, 1]`, which IndexErrors on a single-class classifier (1-column proba) and aborted a valid prediction run — now shape-guarded + wrapped (degrades to a warning). (3) `HurdleRegressor.predict`/`predict_binary`/`predict_magnitude` call `check_is_fitted` → raise `NotFittedError` (was a bare `AttributeError`) when used before fit. (4) `HurdleRegressor.set_params` applies top-level params before nested and RAISES `ValueError` (sklearn semantics) when a `<estimator>__param` targets a `None` sub-estimator, instead of silently dropping it. +6 tests. (Deferred: the tree-winner CV-R²=0.0 metadata honesty — entangled with the #65 gate.)
- [x] **LOW batch — dtype-overflow guard, MR-overlap boundary, MR H3-floor CSV consistency (PR #167).** Three fixes, no shipped-cutoff numbers moved. (1) `optimize_dtypes` no longer casts an int64 column to uint32/int32 when its values exceed the 32-bit range — those columns stay `int64`, so a large euro-cent sum (> 4.29e9) can't silently WRAP to a tiny wrong value; in-range columns downcast exactly as before. (2) The MR-overlap leakage warning uses `start_mr <= end` (was strict `<`): since the pipeline date filters are inclusive on both ends, `start_mr == end` shares the boundary day between main and MR — a one-day leak the old check let through unwarned. (3) The MR summary H3-floor clamp now also rewrites the persisted `todu_30ever_h6` numerator to `(h3/100)·den/multiplier`, so a CSV consumer recomputing `multiplier·num/den` reproduces the displayed (floored) risk instead of the old un-floored value (MR monitoring; only fires when the floor already clamped the shown rate). +5 tests (3 dtype-overflow, 2 MR-overlap) + extended the existing H3-floor summary test to assert numerator consistency. Also recorded: the "`variables` ≥2" finding is inverted (1-D configs are supported now); MAD-constant / quantile-tie / out-of-range-gate / `calculate_annual_coef` are decision-relevant, not report-only.
- [x] **LOW batch — metrics diagnostic honesty (PR #166).** Three report-only metrics fixes, no numbers moved on the current path. (1) Both bootstrap CIs (`bootstrap_confidence_interval`, `_bootstrap_ci_combined`) now surface how many resamples were single-class and dropped (shared `_warn_if_many_bootstrap_skips`; warn when >20% skipped) — a CI resting on a handful of resamples no longer reads as solid. (2) `compute_score_discriminance` counts `n_records`/`n_bads`/`bad_rate` on the **valid** (non-NaN-score) subset the gini/KS was actually computed on, not the full population — a combined OOF score that's NaN on unfittable-fold rows no longer mislabels its sample size. (3) Documented the combined-score OOB CI as a deliberately-conservative lower-leaning band (replicates train on ~63% + score on OOB, vs the K-fold OOF point estimate) rather than a symmetric bracket. +3 tests. Closes the Metrics LOW bullet (with #163/#164).
- [x] **LOW batch — diagnostic NaN/empty-safety + immature-tail (PR #164).** Three report/diagnostic-only fixes, no cutoff path. (1) `metrics.calculate_psi_by_period` now drops NaN scores from **both** windows before counting (NaN in the reference used to poison every `np.percentile` breakpoint → forced NaN PSI; NaN in the actual deflated the denominator → understated PSI) and guards empty windows → a single-row NaN frame (was a 0/0 → inf/NaN percentages). Shared `_nan_psi_frame()` helper reused by both the empty-window and <2-breakpoint branches. (2) `selection_bias.compute_ri_gini` inherits the nearest covered decile's rate for fully-rejected bands (`_nearest_decile_rate`) instead of the old `np.zeros` all-good default — the riskiest deepest-reject tail is no longer imputed as risk-free. (3) `trends.compute_monthly_metrics` gained opt-in `maturity_months` blanking of the H6-immature `risk_rate` tail (threaded from `settings.mr_maturity_months` in main.py; default `None` = unchanged). +6 tests.
- [x] **LOW batch — monitoring/provenance robustness (PR #163).** Five report-only/diagnostic hardening fixes with no cutoff path, batched from the LOW list. (1) `stability.calculate_psi` empty-population → **NaN** (not `0.0` = false STABLE), aligning with #12's undefined→NaN→UNSTABLE policy. (2) `metrics.bootstrap_confidence_interval` all-degenerate-resample case → **NaN CI** (not the `(0.0, 0.0)` sentinel that reads as a confident zero-width CI). (3) `backtest`: drift flag now returns **BELOW** when the OOT risk CI sits entirely below in-sample (was one-sided → "OK"); per-cell calibration `None`→`np.nan` (fixes object-dtype subtraction TypeError); docstring states the loan-level-iid vintage-clustering CI limitation. (4) `mr_pipeline` fallback stability vars now drawn from a **sorted** shared-column set (bare `set` iterated in hash-randomized order → non-reproducible PSI). (5) `lineage.git_provenance` runs git with `cwd=` the source tree (was process CWD → a wrapping repo could stamp its commit/dirty flag). +4 tests (2 stability updated to assert NaN, +metrics NaN-CI, +backtest BELOW).
- [x] **#65 — Deployed pooled risk model was a CONSTANT predictor (no discriminatory power) (PR #159).** Found 2026-08-14 via direct inspection of `output/_supersegment_total/models/model_20260630_122828/`: the shipped pooled model was `XGBoost (Optuna Tuned)` with `min_child_weight=22`, 127/127 leaf-only trees, every bin predicting `0.0243`, `cv_mean_r2=0.0`, `train_r2≈2.2e-16` — a FLAT risk surface, so the MILP had zero model-derived differentiation between score bins. Root cause: trees train on aggregated bins (one row/bin) with weights normalized to sum = n_bins, but the `min_child_weight` / `min_child_samples` search range was a fixed `[10, 100]` — so on the ~10-bin pooled grid every leaf floor exceeded the total weight and no split was possible (the specific artifact predates the #46 weight-unification fix, but the fixed range is degenerate on any small grid regardless). The pipeline shipped it silently (merely logged `Train R²: 0.0000`). **Fix (two parts):** (1) dynamic leaf bounds — `min_child_weight`/`min_child_samples` search ceiling now scales to the bin count (`max(2, min(100, n_bins // 3))`, lower 1) instead of `[10, 100]`; (2) hard gate — `_check_model_discriminates` rejects (`DegenerateModelError`) any final model whose predictions span < 0.1% of the target's range across the grid (model-agnostic; catches zero-split boosters + any constant model), so a flat risk surface halts the run loudly instead of shipping. +4 tests. **Action: the committed pre-fix pooled model must be RETRAINED** — the current deployed artifact is the degenerate one.
- [x] **#46 — Two divergent `_get_regression_weights` implementations.** `optuna_tuning.py:23-27` (raw exposure) vs `inference_optimized.py:350-366` (negative-clip + p99 cap + normalize) — model-type/tree selection fits under different weighting than feature-set CV and the final fit; stage RMSEs not comparable.
  - **Done (PR #148):** deleted optuna_tuning's local copies of BOTH `_get_regression_weights` **and** `_stratified_cv_splitter` — the latter was still the pre-#33 version (stratify on `indicators[0]` → silent KFold), so #33's "all consumers go through one splitter" claim didn't actually hold for model-type/hyperparameter CV. Both now lazy-imported from inference_optimized; `evaluate_holdout_rmse` stratifies via new shared `_stratify_labels`. Third-pass analysis: raw €-scale weights made the searched `alpha` effectively inert during tuning (weight scale ÷ alpha equivalence) and flipped XGBoost `min_child_weight` from inert to drastic at final fit — tuning now optimizes the real objective, so tuned hyperparams/model type may legitimately change on real data (**re-validate at the next M5 re-pin**).
- [x] **#56 — Final-fit target-based outlier removal → winsorize + configurable (PR #158).** `process_dataset` no longer DROPS bins whose realized risk (the target `b2_ever_h6`) is extreme — it **winsorizes** them (clips the target to the `±z_threshold` boundary) so every observed bin keeps influencing the deployed fit while a single corrupt bin is still bounded. The old drop discarded the high-risk tail from the fitted surface (on right-skewed credit data), letting the smooth model extrapolate LOWER risk into exactly the cutoff-sensitive cells — anti-conservative, and worse on the small single-var retail grid (2 of 12 bins removed). Also made it configurable: `z_threshold` is now threaded from config into `inference_pipeline` (was hard-coded `DEFAULT_Z_THRESHOLD=3.0` at all 3 internal call sites) and the validator relaxed `gt=0 → ge=0` so `z_threshold=0` disables outlier handling. **Decision-relevant** (moves the fitted surface → cutoffs) — validate real-data before/after. +3 tests; CLAUDE.md updated.
- [x] **#45 + #64 — Consolidated-HTML standalone segments + standalone-CLI config drift (PR #157).** #45: `_portfolio_group_list` now always appends the standalone `segment_*` groups (was only when NO supersegment existed → they vanished from portfolio sections while staying in TOTAL, so sections stopped reconciling). #64: the backtest + policy-registry CLIs now honour the configured `sas_encoding` (was hard-coded latin-1); `backtest_all` defaults to each segment's own `mr_maturity_months` (was hard-coded 6; explicit `--maturity-months` still overrides all); `run_score_metrics` honours per-segment date-window overrides from segments.toml and dedups loans (on `authorization_id`) before pooling supersegment/total metrics so overlapping filters don't over-count n / understate SEs. Report/CLI-consistency only, no cutoff impact. +5 tests.
- [x] **#38/#55 — Report-only sensitivity fixes (PR #156).** `perturb_risk_summary` now scales the TOTAL risk numerator `todu_30ever_h6` (and its `_boo`/`_rep` components proportionally), not just `_rep` — booked-driven cells can finally flip, so `flip_threshold_pct` no longer overstates robustness. `run_sensitivity_phase` reuses the base scenario's frozen `acceptance_mask` (decoded from `optimal_solution_base.csv`) as the baseline instead of re-solving a fresh MILP at the raw unrounded `optimum_risk` — the baseline is now the shipped policy (honoring `ci_upper` and `fixed_cutoffs`), with a re-solve fallback only when the frozen mask is unavailable. Sensitivity moved from step 6b to 6e (after the RI optimizer's re-optimization) so it describes the final surface. All report-only (no cutoff/gate consumes sensitivity). The decision-relevant rounding half (#38 item 1) stays open. +3 tests.
- [x] **#59 — Selection-bias diagnostics contaminated by canceled applications (PR #155).** `~booked` counted canceled apps (score-accepted, not taken up) as rejected across five computations. Now: `compute_distribution_truncation`'s rejected set is `status == rejected` (not `~booked`); `compute_rejection_profile`, `compute_booked_vs_rejected_discriminance`, and `compute_ri_gini` exclude canceled from their populations/deciles; and the runner's cross-group acceptance rate uses `booked / (booked + rejected)`. Separately, the Thorndike Case-2 correction is now fed the **score-rejection-only** u-ratio (analysis 8, `booked + 09-score`) instead of the all-demand analysis-1 u-ratio widened by score-orthogonal 08-other + canceled removals — the module already computed the right input but never used it. Diagnostic-only (no cutoff/RI/gate impact). +5 tests (canceled excluded from each population; Thorndike uses the score-only u-ratio).
- [x] **#58 — RI calibration floor mismatch (PR #153).** The optimizer's calibration target clipped effective acceptance at 0.05 while the runtime power-method uplift clipped at 0.01 — for bins in (0.01, 0.05) (the deepest-reject bins) the optimizer scored uplifts against a rate up to 5× the one actually applied, defeating the shared `_shrink_acceptance_rate` helper. New shared `MIN_EFFECTIVE_ACCEPTANCE_RATE = 0.01` used by both the runtime clamp and the optimizer target (+ the anchor floor + near-zero diagnostic). **Decision-relevant** (raises deep-reject uplift targets → more conservative cutoffs, `run_ri_optimizer=true` only) — validate real-data before/after. +2 tests.
- [x] **#41 — Backtest point estimate vs CI on different NaN rules + one-sided holdout (PR #154).** `_realized_metrics` summed the risk numerator/denominator per-column (NaN-skipping) while the bootstrap CI dropped loans row-wise, so a loan with a valid default but NaN exposure (incomplete H6) was in the point but not the CI — the CI bracketed a different estimand. Now one completeness mask (both `todu` fields present) drives the risk point AND the CI; the excluded count is logged + surfaced (`n_incomplete_h6_oot`). Since `compare_policies` reuses `_realized_metrics`, the policy-registry point now matches its paired bootstrap too. Also: a one-sided `--holdout-start`/`--holdout-end` (was silently ignored → auto-derived) now fails fast in both `backtest_segment` and `compare_segment`. (Age-completeness is already handled by the maturity window; per-cell calibration diagnostic left as-is — the audit targeted the headline.) +3 tests.
- [x] **#50 + #62 + #49 (core) — management-facing display & aggregation fixes (PR #152).**
  - **#50 (allocator 100× risk):** `interactive_allocator` formatted already-in-percent `b2_ever_h6`/`global_risk` with `:.2%`, rendering 1.5% as "150.00%" on the KPI card and results table. Changed to `:.2f}%`. (Dead "Locked" dropdown left open — usability gap, not a wrong number.)
  - **#62 (fabricated pooled rates):** `_rejections_from_rows` used `fillna(0)` so a segment without `se_decision_id` contributed €0 rejections while its demand stayed in the pooled denominator — understating the supersegment/TOTAL system-rejection rate and showing a fabricated "0.0%" when no member had it. Now None-poisons on any NaN member rate (→ "n/a"), mirroring `aggregate_metrics`. Also gated the baseline classification grid off (main + MR) — baseline segments' accept-all mask made it show phantom swap-in € contradicting the zero-swap RP table above it.
  - **#49 (risk surfaces fabricate on missing columns):** `classify_cells` raised the flat-0/empty-surface risk — a missing `todu_30ever_h6` silently became `np.zeros` → a plausible flat-0% surface on the management deck. Now raises `ValueError` on a missing required risk column; the 3 callers (`plot_risk_surface`, `generate_results_presentation` ×2) skip that surface with a visible warning; a missing booked-before column warns. (booked-before proxy switch + `dropna` transparency left open — touch the Inigo-confirmed design, confirm first.)
  - +5 tests; suite 1419 pass. Display/aggregation only — no cutoff impact, so no real-data run required.
- [x] **#51 + #54 + #52(a) + #53(part 1) — anti-conservative MR/preprocessing fixes (PR #151).**
  - **#51 (MR H6/H3 ratio clip guard):** the degenerate/inverted percentile band reset only `upper_q` to 5.0, so `np.clip(x, lower_q>5, 5.0)` collapsed every ratio to 5.0, understating extrapolated MR risk. Extracted `_robust_ratio_clip_band` (unit-tested) that widens a degenerate band to bracket the observed ratios (no-op clip) instead of clamping.
  - **#54 (stress factor → 0):** a zero worst-slice denominator gave `worst_bad_rate=0` → stress factor 0, which multiplied predicted risk to zero. Both `calculate_stress_factor` and the per-bin variant now return the neutral 1.0 / global fallback with a warning (matching the other degenerate branches).
  - **#52(a) (maturity day-precision):** `_compute_account_maturity` now counts COMPLETE months (day-of-month aware), so loans weeks short of maturity no longer pass the `>= mr_maturity_months` gate and dilute `b2_mr` with immature zeros; matches the M4 backtest. Removed a duplicated inline `(year,month)` diff. Existing #2 regression tests reference-aligned to the 15th (day-precision now excludes a genuinely-immature loan the old arithmetic over-counted — the fix working).
  - **#53(part 1) (oa_amt_h0 ordering):** `_run_data_transformations` now runs the status relabel BEFORE `update_oa_amt_h0`, so direct-measure rejections (the #141 population) carry the demanded `oa_amt`, not the booked H0, in every €-weighted rejection/swap-in/RI denominator. (Proper dependency order — `update_oa_amt_h0` reads `status_name`.)
  - +8 tests; suite 1414 pass. **Real-data before/after not run** (new dataset): #53(part 1) is decision-relevant (feeds `oa_amt_demand` → swap-in production) — validate deltas on the new data before relying on the numbers. #51/#52(a)/#54 move MR/stress numbers only in their degenerate/leaky branches (monitoring + conservatism).
- [x] **#48 — Resimulation failures are silent successes + #61 — failed segments' stale artifacts leak into deliverables (PR #150).**
  - **#48:** `run_resimulation` now **raises `ResimulationError`** on missing artifacts/model instead of `return None`, so `run_batch`'s per-segment `except` marks the segment failed (was never caught → old artifacts consolidated as "resimulated", exit 0) and the CLI exits non-zero. New `cleanup_stale_scenarios()` (disk-discovery) replaces the full-run's hardcoded `{_pessimistic,_base,_optimistic}` block — it now also purges orphaned `target_X.XX` files and covers `accepted_cells_*`/`audit_*` — and adds the previously-absent resimulation cleanup (`--resimulate 1.0` no longer leaves stale pessimistic/optimistic; `"base"` protected as the optimization input, not just a report output).
  - **#61:** consolidated **HTML** built from successful segments only (`generate_batch_reports`; the Excel path already filtered — the two could disagree); resimulation consolidation excludes failed segments; `main()` returns a `False` sentinel on `PipelineExecutionError` and the CLI `sys.exit(1)`s (was always exit 0); sequential-ordering floor cells **warn loudly** when the constraint source didn't run this batch (prior-run staleness) rather than silently using them — kept a warning, not a hard fail, to preserve incremental re-runs (file-missing still hard-fails, #36). Both sequential + parallel paths.
  - **Partial:** #61's "stamp+verify floor-cell provenance" is a warning, not a cryptographic stamp — a full provenance check is a deferred refinement (folds into #40). +7 tests; suite 1406 pass.
- [x] **#31 extension — Policy registry fail-closed on unfrozen bin edges + corrupted registry.**
  - **Done (PR #149):** `build_policy_entry` refuses (ValueError) when any configured bin variable lacks explicit `bin_edges` (learned `max_bins` edges are never persisted — the old code froze `bin_edges={}`, and the compare-time #31 guard `if champion.bin_edges and ...` was skipped exactly when the pin was missing); `compare_segment` refuses (`sufficient=False`) when current bins are unfrozen (`_run_data_transformations` would RE-LEARN edges on today's population and score different score regions) or the champion carries no frozen edges; `load_registry` raises on a corrupted committed registry instead of treating it as empty (the next `--register` wiped history and auto-championed the new policy). New shared `unfrozen_bin_vars()`; +5 tests. Pre-fix champions with empty `bin_edges` must be re-registered.
- Housekeeping: the second-pass note about a stray debug `print(data_path)` in `data_manager.py` is resolved (no longer present).

## Fixed — second pass (2026-06-10)

Six parallel domain audits (statistics/RI, optimization, preprocessing/batch, modeling/CV, validation/governance, reporting/aggregation). Recurring themes: (a) **optimism by construction** — the winner's-curse shape repeats across cutoff selection, model selection, holdout evaluation, RI tie-breaking, and the backtest baseline; the M4 backtest is the only genuinely out-of-sample number; (b) **fail-closed in name, fail-open in fallbacks** — preload failure, MILP fallbacks, missing floor-cell artifacts, missing SHAs all degrade silently; (c) **same concept, multiple implementations** — segment matching (3 semantics), regression weights (2), backtest NaN rules (2).

- [x] **#22 — Phantom grid cells poison the MILP monotonicity constraints.** `optimization_utils.py:372-381` builds adjacency constraints over **all** cells in `cell_index` (full Cartesian grid) while `:541-546` pins unobserved (phantom) cells to x=0. A phantom acting as the "safer" endpoint force-rejects every observed cell coordinatewise-riskier than it; a phantom near the safe corner can make whole regions/sweep targets infeasible and silently route into the GA/legacy fallbacks (see #36). The uncertainty relaxation can't rescue these pairs (zero exposure → NaN risk → `ambiguous_pair` stays False). *(Verified in source.)*
  - **Done:** `_build_monotonicity_constraints` now links **consecutive observed cells per 1-D line** along each dimension — adjacent neighbors on a full grid (constraint set unchanged), bridged pairs across phantom gaps on sparse grids; phantom cells participate in no constraint (they stay pinned x=0 by bounds, contribute 0 to objective/KPIs; `classify_by_mask` still fail-closes unseen cells). Both consumers fixed at once (MILP + GA fallback share the builder). Known relaxation: an observed pair separated by an all-phantom "diagonal wall" (every lattice path blocked) is left unconstrained — fail-safe direction (no force-reject), and no data sits between them anyway. Debug log reports the bridged-pair count.
  - **Failure mode confirmed:** the new regression test run against the OLD code shows a phantom at the safest corner of a 3×3 grid propagating x=0 through the chain to every cell → every MILP target infeasible (`None`); with the fix the solver returns the normal solution. +6 unit tests (phantom column untouched, gap bridged, inverted-direction bridging, full-grid constraint-count parity, safe-corner regression, monotonicity preserved over observed cells). Suite 1320 pass; ruff lint+format clean.
  - **Real-data before/after** (`no_premium_cd`, 1.44M rows, reused supersegment model, RI optimizer + sensitivity off to isolate): the real 20×10×2 grid has **4 phantom cells** (396/400 observed) — three of them ((1,9,1), (1,10,1), (2,5,1)) sit in the safest-octroi bins of risky-efx rows, each force-rejecting its whole line under the old code. **Base optimum byte-IDENTICAL** (sol_fac 41, risk 1.092314%, production €53,192,460, 159 accepted cells — the force-rejected cells are ones the optimizer rejects anyway at the 1.1% cap). **Pareto frontier strictly enlarges at the loose end**: 112 → 113 points, max production €74.354M → €74.613M (+€258.8K), max frontier risk 1.8862% → 1.9011% — the recovered chains. Zero impact on the shipped base cutoff for this segment; a segment/target where a phantom binds nearer the cap would previously have been silently infeasible (or mis-shaped) — that's the case the fix exists for.
- [x] **#23 — Selection-bias range-restriction analysis truncates the WRONG tail.** `selection_bias.py:229-237`. Module convention is `sign·score` = higher-is-riskier; the code sorts descending and keeps `[:n_keep]` — retaining the **riskiest** and removing the safest, opposite of the documented "simulate a tighter cutoff". The Gini-decay-vs-truncation curve (the centerpiece of that report) is computed on the inverted restriction; the unit test only asserts monotone decay, which holds either way. Diagnostic-only (no cutoffs derive from it) but the headline analysis is wrong. *(Verified in source.)*
  - **Done:** ascending sort (drop the `[::-1]`) so `[:n_keep]` retains the safest pct and truncation removes the riskiest tail; misleading comment + docstring corrected. Consumers (plot, `gini_drop_pct` summary) are direction-agnostic — no other changes. +1 direction-discriminating regression test via `n_bads`: on old code it fails with **334/386 bads retained (87%)** after "truncation" to 50%; the decay-only test passes on both (as the audit predicted). **Real-data before/after** (full `run_selection_bias_analysis`): before, 50% "truncation" kept **67–72% of bads** across segments/scores (riskiest half retained); after, **28–33%** (riskiest half removed). The headline Gini attenuation changes materially — e.g. `total/Risk Score RF` drop at 50% truncation **0.0437 → 0.2184** (the report understated range-restriction decay ~5×). Any previously generated selection-bias report's range-restriction panel is wrong — regenerate on next use (nothing committed; `output/` is untracked).
- [x] **#24 — Management deck per-segment charts double-count supersegments.** `presentation_charts.py:40-43` (`_segments_frame`) excludes only `group != "TOTAL"`, but the consolidated CSV also carries `supersegment_*` rows alongside member rows → production/risk/acceptance bars and the waterfall fallback plot the same euros twice; bars no longer sum to the exec-summary total. Fires on the next batch run (every segment now has a `reporting_supersegment` in segments.toml). *(Verified in source.)*
  - **Done:** `_segments_frame` now excludes `supersegment_*` rows alongside TOTAL (same predicate as `create_consolidation_dashboard`); all four deck chart builders (production/risk/acceptance/waterfall-fallback) go through it. **Real-artifact before/after** (real `consolidate_segments` over the existing `output/` segment artifacts with a single-member + a multi-member reporting supersegment, mirroring the live segments.toml shape): BEFORE the chart frame contained both supersegment rows and their members — by-segment bars summed to **€170.27M vs the €99.74M TOTAL row** (€70.5M double-counted); AFTER the bars sum to **€99.82M = the TOTAL row exactly**. +1 regression test (fails on old code). Suite 1323 pass; ruff clean.
- [x] **#25 — Audit-based production patch lands on the wrong row for single-member supersegments.** `consolidation.py:692-700` locates rows by `segments == seg_name` only; for a one-member supersegment (live case: `pl_new`/`new_nofintonic_ob`) the supersegment row matches first and gets patched, then the re-summation loops (707-748) overwrite it from the **unpatched** member row — the loan-level audit correction is dropped and TOTAL mixes reconciled and raw bases.
  - **Done:** the per-segment patch mask is now restricted with `_detail_segment_mask` (helper hoisted above the loop), so audit KPIs always land on the detail-segment row; the supersegment/TOTAL re-aggregation then propagates the **patched** numbers. **Real-artifact before/after** (same harness as #24; `new_no_fintonic` as the single member): BEFORE the member row kept its stale RP-table numbers (actual €5,408,662 / optimum €3,727,012 — the audit patch was swallowed by the aggregate row and overwritten); AFTER the audit-reconciled numbers flow member → supersegment → TOTAL (actual €5,485,662 / optimum €5,052,735; **TOTAL optimum €77.00M → €78.33M, +€1.33M** — the previously-lost correction). Multi-member supersegments verified unchanged in the same run. +2 regression tests (single-member fails on old code; multi-member control passes on both). Suite 1323 pass; ruff clean.
- [x] **#26 — Reproducibility data-SHA pin passes vacuously.** `reproducibility.py:146-153`: `snapshot_match` only goes False when *both* sides carry a SHA and differ; lineage is best-effort by design, so a lineage hiccup silently removes the snapshot pin from a gate documented as "PASS iff … SHA matches". Same pattern at :121-136 (`risk_ok`/`prod_ok` default True when the value is missing). *(Verified in source.)*
  - **Done (fail-closed):** `compare_headline` now FAILS when the SHA is missing on **either** side ("DATA SNAPSHOT PIN UNAVAILABLE … re-establish the reference with lineage enabled") and when `risk_pct`/`production_eur` is missing on either side (no more vacuous pass on the remaining checks). The committed reference carries a SHA, so the legitimate path stays passable. The old `test_missing_sha_does_not_block` (which encoded the vacuous pass) is inverted to `test_missing_sha_fails_closed` + a missing-numbers test. **Real-data check (`run_reproducibility -s no_premium_cd`, reused model):** the SHA pin actively verifies under fail-closed (`snapshot_match=True`, sha `7cce2ceba083` matches) — and the gate **FAILS loudly on the numbers** (risk Δ +0.0102pp just over the 0.01pp tol, production Δ +4.82%, 96→95 cells): the **committed reference is stale** w.r.t. current main (it predates PR #89, whose phantom-monotonicity fix legitimately shifted the frontier; this posture runs the RI optimizer where that propagates). Not a regression of this fix — re-validate and `--update-reference` is a pending governance decision (see the open Governance action).
- [x] **#27 — Batch preload failure silently swaps the binning methodology.** `run_batch.py:1573-1583`: global + supersegment bin edges are computed only when `preloaded_data is not None`, and `load_and_standardize_data` returns `None` on *any* exception with a warning. The hand-configured **fixed** `income_bin` edges in segments.toml flow only through `learn_supersegment_bin_edges`, so a transient load failure makes every segment fall back to per-segment quantile-learned income bins — different grids, different cutoffs, no error.
  - **Done (config always flows + fail-closed gate):** `learn_supersegment_bin_edges` now accepts `data=None` and extracts **fixed** supersegment edges unconditionally (they're configuration, not data-derived); also removed the early return that dropped fixed edges whenever no global bin was learnable (a second latent path to the same loss). The batch gate: when preload fails and any bin was supposed to be **learned** on the shared population (globally or `learn_own_bin_edges`), the run **halts with exit 1** instead of degrading to per-segment learning. **Real-data demo (live config + broken `data_path`):** new code extracts all four supersegments' fixed income edges, then aborts with the audit-#27 error; old code logged a warning and sailed into "PHASE 1: Training Supersegment Models" unconstrained — the silent methodology swap. +4 unit tests (3 fail on old code).
- [x] **#28 — Winner's curse in the reported optimum; bootstrap CI doesn't cover the headline.** The pipeline solves ~50+ MILPs, picks the max-production mask whose **point-estimate** risk is under threshold (`plots.py:746-767`) and reports that same in-sample estimate — biased optimistic exactly at the binding boundary (noise-underestimated cells preferentially accepted). The bootstrap CI (`utils.py:545-561`) resamples loans under the *fixed* chosen cutoff (ignores the selection step) and resamples **booked-only** data while the point estimate includes RI+stressed swap-in (`scenarios.py:243-249`) — the CI brackets a different quantity than the number it decorates (repesca production added as a constant to every replicate). Related: the M4 DRIFT baseline (in-sample realized under the optimized mask, `backtest.py:62-80`) inherits the same optimism, so regression-to-the-mean can masquerade as drift. Delivered in 3 PRs: Phase A (CI basis), Phase B (selection-aware CI + optimism estimate), Phase C (`ci_upper` selection, gated OFF per Inigo's call). The M4-baseline optimism remains documented (narrative) — full M4 redesign is the audit's separate backtest finding.
  - **Phase A done (CI basis — report-only, PR #94):** `calculate_bootstrap_intervals` now resamples the score-rejected population alongside booked (`_RepescaBootstrapContext`: each accepted cell's final `*_rep` values scale with the resampled/original rejected-amount ratio — exact for production, first-order for risk under cell-deterministic RI rates) and computes `risk_ci_*` on the **blended booked+RI basis matching the headline**; `risk_ci_basis` labels it, `risk_booked_ci_*` preserves the booked-only realized CI; legacy path (no rejected data) byte-stable + labeled. Dead `model_cv_se_risk` param removed. Exec-summary narrative + assumptions register state the winner's-curse caveat and point to the M4 backtest as the unbiased check. **Real data (`no_premium_cd`):** headline byte-identical; old risk CI [0.6053, 1.3391] silently bracketed booked-realized ~0.97% while decorating the blended 1.0923% headline — new CI **[0.8044, 1.4344]** is centered on the number it decorates; booked CI preserved bit-identically; production CI width 2.39M → 2.54M (repesca variance included). +5 tests (4 fail on old code).
  - **Phase B done (selection-aware uncertainty — report-only, PR #95):** new `src/selection_uncertainty.py` re-runs the SELECTION inside each replicate over the frozen frontier at cell level (booked resampled→cells; rep component resampled; every candidate mask a dot product; point rule re-applied; ~seconds for 1000×112). Outputs on the Optimum row: `risk_ci_sel_*` (CI of the optimized headline incl. re-selection), `selection_optimism_pp` (Efron-style winner's-curse estimate), `selection_reselect_pct`, + per-candidate CI for Phase C. Documented limitation: conditional on the frozen candidate set (no per-replicate MILP re-solve) — a lower bound. **Real data:** headline byte-identical; selection-aware CI **[0.9325, 1.0995]%** (the procedure's reported risk concentrates under the 1.1% cap — re-selection re-targets the threshold), the chosen MASK flips in **98.8%** of replicates (boundary argmax is noise-driven), optimism **+0.022pp** on this large segment. Found+fixed in passing: loan-level NaN todu_* poisoned bincount cell sums (all-NaN CI on the first real run) — nan_to_num + regression test. +6 tests.
  - **Phase C done (`ci_upper` noise-margin selection — default OFF, PR #96):** `selection_risk_basis` config flag (`"point"` default | `"ci_upper"`, Expert tier, pydantic-validated). When `"ci_upper"`, the scenario selection requires the candidate's per-candidate bootstrap risk CI-UPPER ≤ target (via `select_with_ci_margin` → `target_sol_fac`); no qualifying candidate → loud fallback to the point rule. **Real data:** flag OFF **byte-identical**; flag ON selects sol_fac 5 (risk 0.6358%, ci_upper 1.0803% ≤ 1.10%) instead of sol_fac 41 — production **€53.2M → €20.6M (−61%)**, cells 159 → 44: the honest price of a 97.5%-confidence noise margin at the cap, now a visible, governed choice (documented in CLAUDE.md + assumptions register; enabling requires M5-style sign-off). +3 tests.
- [x] **#29 — RI optimizer calibrates against an inconsistent target + anti-conservative tie-break.** `reject_inference_optimizer.py:118-195`: the predicted side carries the **stress factor** (applied in `compute_pre_reject_inference_data`, inference_optimized.py:1918-1948) and **tasa_fin** scaling; the calibration target is raw, count-based — so the chosen uplift systematically cancels the stress factor, silently removing intended conservatism, and depends on the financing rate which has nothing to do with selection bias. Tie-break (`:290-293`) takes `idxmax(oa_amt_h0)` within the 5% tolerance band — by construction the least conservative risk assumption noise allows. (Extends closed #3/#5, which fixed the acceptance-rate population but not the stress/tasa_fin basis.)
  - **Done:** `evaluate_ri_params` now scores candidates on a **calibration-pure blend** — built from a stress-free repesca frame (`OptimizerInputs.repesca_pre_ri_calibration`, computed via a second `compute_pre_reject_inference_data(stressor=1.0, per_bin_stress=None)` only when stress is active) and **without tasa_fin weighting** — the same raw-demand basis as the 1/a^γ target; the production blend feeding MILP/KPIs keeps stress + tasa_fin (real economics). `_select_best` tie-break flipped to **conservative**: lowest production within the 5% band (deterministic secondaries: uplift desc, cap asc) with a log of band size + production spread. Module docstring now states the honesty caveat (the target is itself an assumption — calibrated, never validated; M4 is the realized check); assumptions-register row added. Both grid + Optuna paths flow through the same code.
  - **Real-data (`no_premium_cd`, optimizer ON, grid 11×9):** (a) **stress cancellation demonstrated and fixed** — BEFORE, enabling `stress_mode="global"` dropped the chosen uplift **4.58 → 3.32 (−27%)** (the optimizer gave the stress conservatism right back); AFTER, the chosen uplift is **invariant to stress** (5.00 with and without). (b) **Live posture** (stress disabled): chosen params 4.58/4.50 → 5.00/5.00 (tie-break: 4 candidates in the band, €911K production spread — old took the max, new the min); headline risk 1.0960% → 1.0988% (under cap), production **€42.65M → €41.49M (−2.71%)**, cells 95 → 93 — the conservative correction, folds into the pending M5 re-pin. Under stress the corrected composition is production −13.0% (stress + RI now compose instead of cancelling). +3 regression tests (all fail on old code). Suite 1354 pass.
- [x] **#30 — MR Tier-1 "actual H6 outcomes" are dead code.** `mr_pipeline.py:986-992` (`_assign_tiered_risk`) restores actual H6 columns for mature accounts, but `process_mr_period` (`:2113`, `:2162`) unconditionally wipes and refills them for **all** booked accounts from model-predicted exposure — mature accounts get `num_i·(pred_pile/actual_pile)` (mean-of-ratios) instead of their actual ratio-of-sums. MR is validation-only (M3a) → monitoring accuracy, not cutoffs.
  - **Done:** new `_restore_tier1_actuals` runs after the model refill in `process_mr_period` and puts the **actual** H6 numerator AND denominator back for Tier-1 (mature) accounts, so their bins aggregate to the actual ratio-of-sums; model-predicted exposure now applies to immature tiers only. **Found in passing:** `_assign_tiered_risk` was dropping the `_actual_*` columns at its end, which would have made the restore a silent no-op — the cleanup is moved to after the restore, with a column-lifetime regression test. **Real data (`no_premium_cd`, MR window 2025-06→2026-02):** the restore fires for **931 mature accounts**; main headline byte-identical (MR is validation-only); the MR monitoring numbers correct upward — realized MR "Actual" risk **1.6083% → 1.6900% (+0.082pp)**, Swap-out 7.51% → 7.68%, Optimum ~unchanged — i.e. the mean-of-ratios reconstruction was **understating realized MR risk**, which matters for every drift comparison anchored on MR realized values. +3 tests (ratio-of-sums restore, no-op guards, column lifetime) — all fail on old code. Suite 1361 pass.
- [x] **#31 — Policy registry: verdict structurally locked at INCONCLUSIVE + challenger survivorship + unvalidated bin edges.** (a) `policy_registry.py:278-296` requires full separation of two *marginal* CIs computed on largely the same loans (strongly correlated) — `Var(Δ)` is far smaller than implied, so genuine differences essentially never separate; needs a **paired bootstrap on the difference**. (b) `run_policy_registry.py:176-189`: challenger scored only on loans booked under the incumbent — its added (riskier) cells are unobservable, biasing toward "same/BETTER"; report unobservable exposure share and block BETTER when material. (c) Champion's frozen `bin_edges` are persisted (`:152-153`) but never validated against current edges before applying its cell indices — silent policy corruption if edges changed.
  - **Done:** (a) `_paired_bootstrap_delta_ci` resamples the SHARED cohort once per replicate and scores both policies on the same draw — the common loans cancel and the CI is on the difference; `_risk_verdict` now gates on the Δ-CI excluding 0 (defaults gate unchanged; marginal CIs kept as informational). (b) `compare_policies` takes `cohort_demand`, reports `n_added_cells_unobserved` + `unobservable_added_share` (challenger-accepted demand production in added cells with no booked loans) and **blocks BETTER** above 5% (fail-closed block when no demand frame and unobserved cells exist). (c) `compare_segment` validates the champion's frozen `bin_edges` (float/±inf-safe `bin_edges_match`) AND `variables` against the current config, refusing with an explicit message on mismatch. CLAUDE.md updated. *(Third pass hardened (c) further — see the #31 extension under "Fixed — third pass".)*
  - **Real-data demonstration** (champion = the pre-#29 `no_premium_cd` policy (95 cells), challenger = the post-#29 policy (93 cells) — two genuinely different real policies; temp registry, real SAS cohort): on the auto-derived holdout the policies share ~98% of loans — marginal CIs **[0.39, 2.32] vs [0.45, 2.54]** (overlap ~2pp, the old rule could never separate them) while the **paired Δ-CI is [0.0169, 0.1312]pp, entirely above zero** — a +0.06pp real difference resolved (~30× the marginal resolution); verdict correctly held INCONCLUSIVE by the **defaults gate** (8 < 10). On a widened window clearing the gate (16/16 defaults): **old verdict INCONCLUSIVE (locked) → new verdict WORSE** (Δ-CI [0.0149, 0.058]). Survivorship metrics live: 3 added cells unobserved, share 0.06% (below the 5% gate). +7 tests (incl. the marginal-overlap-but-paired-resolves regression, the BETTER-block with/without demand, the edge guard) — all fail on old code. Suite 1357 pass.
- [x] **#32 — Model-selection optimism (residual, post-#7):** (a) validation-fold outlier removal in `optuna_tuning.py:99-104, 266-271` omits `outlier_stats` → val bins filtered using their **own target** (the three other call sites pass it correctly); the riskiest bins are never scored, and stage-2 vs stage-3 metrics are on different bases. (b) The "unbiased" holdout (`inference_optimized.py:1488-1503`) is carved from data the selection already saw — relabel or carve before selection. (c) Tuned-vs-untuned comparison is non-nested CV (re-partitioning removes partition-overfit, not data-overfit) → tilts the 1SE selection toward tuned models. (d) The 1SE rule (`:86-122`) consumes the naive `std/√k` SE that `_nadeau_bengio_cv_se` documents as ~2× too small — eligibility band halved.
  - **Done (with #33, one PR):** (a) both optuna `eval_model`s now compute outlier stats on the TRAIN fold and pass them to the val `process_dataset` (same pattern as the three correct call sites) — the riskiest val bins are scored, and stage-2/stage-3 metrics share a basis. (d) all three `CV Std RMSE` producers (both optuna eval_models + `_select_feature_set_cv`, incl. R² SE) now emit the **Nadeau–Bengio-corrected SE**; `_apply_one_se_rule` docstring updated — the eligibility band is no longer halved. (b) `evaluate_holdout_rmse` relabeled honestly: **post-selection** holdout (the split is carved from data the selection already saw; reduced, not zero, optimism; the M4 backtest is the unbiased number) — docstring, log line and metadata comment all updated. (c) the non-nested residual bias is documented at the fresh-seed comment + assumptions register (nested CV rejected as ~k× cost for a residual the 1SE-with-honest-SE partially offsets).
- [x] **#33 — Stratified CV silently no-ops.** `inference_optimized.py:49-62` (also `optuna_tuning.py:30-48`, `evaluate_holdout_rmse`): stratifies on `indicators[0]` = `acct_booked_h0` (~all 1s on booked data) → `nunique>=2` fails → silent fallback to plain KFold, contradicting the docstring.
  - **Done:** `_stratified_cv_splitter` stratifies on `todu_30ever_h6 > 0` by name (`_STRATIFY_COL`), falling back to the configured indicators and then — **loudly** — to plain KFold. All consumers (model-type CV, feature-set CV, both optuna eval_models, holdout) go through this one splitter. *(Third-pass correction: the optuna duplicate splitter + `evaluate_holdout_rmse` had in fact kept the old code — closed by PR #148, see "Fixed — third pass".)*
  - **Real-data before/after (`no_premium_cd`, full training, RI optimizer off to isolate):** stratification + the val-outlier fix + the honest SE **flip the selected model Linear Regression → Lasso (Optuna α=0.711)** — with the riskiest val bins finally scored, LinReg's CV mean falls outside the corrected 1SE band. Headline: risk ~flat (1.0899% → 1.0892%), production **€50.23M → €52.48M (+4.47%)**, cells 129 → 147. Same caveat as #7: production uses pooled `total`-supersegment training — **validate the pooled selection at the next M5 re-pin** (noted in the assumptions register). +2 regression tests (stratification balance with `acct_booked_h0` all-1s; val-fold outlier stats from train fold) — both fail on old code. Suite 1359 pass.
- [x] **#34 — DQ content checks run file-wide, not per-segment.** `data_quality.py:466-513`: only segment-exists/size/booked-ratio are segment-scoped (#18 fixed booked-ratio); missing-values, outliers, date-range, duplicates use the full multi-segment frame — a small segment can be 100%-missing on a score and pass (diluted), while a defect in a segment you're not running halts everything.
  - **Done:** the inline scoping from `check_booked_ratio` is extracted into a shared `_segment_demand_scope` (segment match + fuera_norma/fraud/legal exclusions); `run_data_quality_checks` now runs the **content** checks (missing values, date range, numeric outliers, indicator values, duplicates) on the segment's demand population, with structural checks (required columns, segment exists/size) staying file-level; empty-segment edge falls back to the full frame (the segment-exists FAIL gates the run anyway), and the scope is logged. **Real data (`no_premium_cd`):** identical verdicts before/after (the segment is clean — 1 outlier WARNING either way), now computed on the correct **144,236-row segment basis** instead of the 1.44M-row file ⇒ zero gate-posture change on current runs. The two failure modes are regression-tested: a 50%-of-file defect in segment B no longer halts segment A (cross-contamination) and a 2%-of-file segment that is 100%-missing on a score now FAILS its own run instead of hiding under the file-wide threshold (dilution) — both fail on old code. +3 tests. Suite 1362 pass.
- [x] **#35 — In-sample logistic "combined score" vs unfitted raw scores.** `metrics.py:343-363, 431-434`: the combined model is fit and evaluated on the same rows (optimistic), and its bootstrap doesn't refit per resample (CI too narrow) — "combined beats single" is not like-for-like.
  - **Done:** new `combined_score_cv` produces **out-of-fold** combined scores (StratifiedKFold; scaler fit on train folds only; NaN for unfittable folds, masked by callers) — used by `model_summary`, `compute_score_discriminance`, AND `run_score_metrics._prepare_scores` (the runner had its own in-sample construction feeding lift/DeLong — same disease one level up). The combined score's bootstrap CI now **refits per resample** and scores the out-of-bag rows (`_bootstrap_ci_combined`) instead of holding the fitted coefficients fixed. **Real data (`no_premium_cd` score metrics):** raw scores byte-unchanged; the Combined Gini drops **0.3791 → 0.3673** (main, ~3% rel inflation removed) and **0.3622 → 0.3038** (MR, ~16% rel — the thin window is where in-sample fitting bites hardest). +3 tests incl. the pure-noise regression (in-sample Gini >0.30 on noise; out-of-fold ~0) — 2 fail on old code. Suite 1367 pass.
- [x] **#36 — Fallback paths silently drop constraints.** GA and legacy enumeration receive no `fixed_cells`, swap-in caps, or nested-mask floors/ceilings (`optimization_utils.py:838-865`, `cutoff_optimization.py:402-456`); ceiling-mode last-resort can return accept-all; nothing post-validates. Related: missing `accepted_cells_base.csv` from a floor segment downgrades nesting to a log warning (`run_batch.py:848-856`); parallel top-down keeps only one dependent per floor segment (`run_batch.py:1079-1083`, `dict[str,str]` vs sequential's list+warning); top-down with multiple dependents should intersect ceilings.
  - **Done:** (a) `_ga_pareto_fallback` now takes `fixed_cells` (encoded as frozen gene bounds) + `max_swapin_production_pct`/`max_swapin_risk` (extra inequality rows, same linearization as the MILP) AND post-checks every returned mask against pins/caps — all three call sites thread them through. (b) The last-resort "accept-all upper bound" now respects ceiling (must-reject) pins and phantom cells; when swap-in caps are also configured a loud warning states the last-resort masks honor only the pins (no fully-feasible solution exists). (c) The constraint-blind **legacy 2-var enumeration is skipped** whenever cell pins or swap-in caps are active — the empty-MILP fallback goes to the constraint-aware GA instead (loud log). (d) Missing `accepted_cells_base.csv` from a constraint source is now a **hard segment failure** ("Refusing to run UNCONSTRAINED") in both the sequential and parallel-2b loops. (e) Parallel top-down `reverse_deps` is `dict[str, list[str]]` matching the sequential path (warning + first dependent; full ceiling-intersection across multiple dependents remains a deferred design improvement — both paths now behave identically and loudly). **Real-data check:** the live config takes the MILP path — full `no_premium_cd` re-run is **byte-identical** (optimal solution, accepted cells, Pareto frontier) ⇒ zero impact on normal runs; the fixes change only what happens when fallbacks fire. +5 tests (GA pins, GA swap-in cap, last-resort ceiling pins, legacy-enum skip, batch bins) — all fail on old code.
- [x] **#37 — `inv_var1` dropped in fixed-cutoff and legacy-enumeration KPI paths.** `cutoff_optimization.py:237-246, 425-435` compute `inv_var1` but don't pass it to `kpi_of_fact_sol`/`get_fact_sol` → with `variables[1]` inverted, frontier KPIs sum the wrong side of the cutoff while audit/bootstrap/MR (scenarios.py builds `CutoffSpec` correctly) describe the right one — same run, two policies. Benign under current config (only var0 inverted), silently wrong otherwise.
  - **Done:** `inv_var1` now reaches `kpi_of_fact_sol` on the fixed-cutoff path and BOTH `get_fact_sol` + `kpi_of_fact_sol` on the legacy-enumeration path; plus the audit's secondary item — `get_fact_sol`'s reject-the-whole-bin sentinel is now direction-aware (`0` under `var1<=cut`; `max(var1)+1` under the inverted `var1>=cut`, where `0` meant accept-all and reject-all was unrepresentable). **Real data:** byte-identical headline on the live config (only var0 inverted — the bug was latent), as expected. +3 tests: KPI sums the `>=` side and adjacent cuts partition the total; inverted enumeration carries the reject-all sentinel; orchestration-level regression that `inv_var1` reaches the KPI call (the unit-level test couldn't catch the call-site omission) — sentinel + orchestration tests fail on old code. Suite 1367 pass.
- [x] **#42 — What-if CSV always records `feasible=True`.** `run_allocation.py:290` never passes the flag to `what_if_dataframe_row` (defaults True) — the #15 feasibility plumbing exists but doesn't reach the management-facing table.
  - **Done:** `what_if_dataframe_row` now defaults its `feasible` field from `result.feasible` itself (`feasible: bool | None = None`; explicit kwarg only to override) — fixes every caller, not just the one call site. **Real-data before/after** (`run_allocation --what-if 0.3,1.2` on the 11 real frontiers): infeasible target 0.3% (greedy best-effort achieves 0.75% > target) flips **`feasible=True` → `False`** in the persisted what-if CSV; feasible target 1.2% stays True; all other columns identical. +1 regression test (fails on old code).
- [x] **#43 — Excel classification-grid "Total" risk: production-weighted mean of percentages with None→0 bias.** `consolidation.py:2371-2379`: weights by `oa_amt` instead of the risk denominator, and None-risk bins contribute 0 to the numerator while their volume stays in the denominator → total biased downward. The same workbook computes per-bin risk correctly from todu sums (`:3356-3388`).
  - **Done:** `_build_classification_grid` now stores each bin's risk denominator (`risk_den` = Σ todu_amt_pile_h6 of the rate); `_write_classification_grid` pools the Total as Σ(risk·den)/Σden — **exact** since risk = mult·num/den ⇒ Σ(risk·den)/Σden = mult·Σnum/Σden. No-risk bins are excluded from both sides (old code counted them as 0% risk); legacy rows lacking `risk_den` fall back to volume weights over risk-bearing bins only. **Real-data verification** (fresh `no_premium_cd` 3-var artifacts from the #22 run — the on-disk segment artifacts predate income_bin-era audits and skip the grid): the new Totals now match `optimal_solution_base.csv` **exactly** — Optimum 1.092314 (= the headline optimum risk), Keep 0.9425 (= b2_ever_h6_boo), Swap-in 1.9837 (= _rep), Swap-out 4.2687 (= _cut) — while the old volume-weighted totals disagreed with all four (e.g. Optimum 1.0883, Swap-in 2.0022). +3 regression tests through openpyxl write+readback (all fail on old code).
- [x] **#44 — Results deck mixes scenarios when `--scenario != base`.** `generate_results_presentation.py:183-195` threads the scenario into backtest/surfaces/title, but `presentation_charts.py` `_total_row`/`_segments_frame` hardcode `scenario="base"` → exec-summary KPIs and all four summary charts show base numbers under a pessimistic label.
  - **Done:** all four chart builders (production/risk/acceptance/waterfall) take a `scenario` param forwarded to `_segments_frame`/`_total_row`; the deck builder passes it everywhere (exec-summary KPIs included) and the exec-summary subtitle now names the actual scenario instead of hardcoding "(base scenario)". A scenario absent from the consolidated CSV now **raises** with the available list instead of silently producing a base-numbers deck under a non-base label. **Real-data before/after** (committed consolidated CSV, which carries all 3 scenarios): a `--scenario pessimistic` deck's exec-summary/charts showed **base** numbers (€277.78M optimum / 1.163%) before; now they show the actual pessimistic scenario (**€72.72M / 0.758%**) — a €205M mislabel on a management slide. +2 regression tests (chart picks the requested scenario's bars; unknown scenario raises; both fail on old code).

## Fixed — first pass (2026-05)

- [x] **#1 — Bin-edge target leakage + wrong population.** `learn_optimization_bins` deprecated (forced quantile); edges now learned on the date-filtered demand population in all 3 call sites (`preprocess_improved.py`, `run_batch.py`). Verified: income edge shifted −20.8% (booked→demand) on real data.
- [x] **#2 — MR maturity anchored to `max(mis_date)`.** H3 path (`mr_pipeline.py:410`) and `_assign_tiered_risk` (`:939`) now use the observation-horizon anchor (`date_fin_book_obs_mr`), consistent with the H6 path. (H6 path was already correct given the end-of-month-next-month data convention.)
- [x] **#3 — RI calibration / acceptance-rate population mismatch.** Root cause was `reject_include_all_rejections=true`: the all-rejects acceptance rate fed both the parceling uplift and the calibration target, but the blend is only score-rejected. Deprecated the flag and forced **score-only** acceptance rates everywhere (`reject_inference.py:compute_acceptance_rates`); fixed the misleading warning that recommended the flag. Original "wrong estimand" framing was overstated — downgraded from HIGH; the objective's intent (blend ≈ selection-model population risk) is sound.
- [x] **#4 — Bayesian/empirical-Bayes smoothing mixes scales under time decay.** `reject_inference.py:compute_acceptance_rates`. Under time-decay the Beta-Binomial posterior was computed on `Σw` (float effective counts shrunk below the raw count), so the count-scale prior systematically over-shrank toward the global rate, and the empirical-Bayes moment estimator (unweighted cross-bin variance, global `p`, `n_i = Σw`) was unsound on a few weighted bins. Fix (Option 2, domain-expert-chosen): under decay the posterior is now computed on the **Kish effective sample size** `n_eff = (Σw)²/Σw²` using the analyst-configured `bayesian_prior_strength` directly, and the EB auto-tuning is skipped — it now runs only on the unweighted integer-count path where it is valid. The no-decay path is **bit-identical** (all existing tests green; added 6 decay-specific tests; the no-decay regression golden was strengthened from a degenerate single-bin case — where `global_rate == bin_rate` makes smoothing a no-op — to a 2-bin case that actually exercises the EB auto-tune). **Real-SAS before/after** (segment `no_premium_cd`, smoothing+decay on, RI optimizer off to isolate the fix, identical trained model): swap-in reject multiplier avg 2.252 → 2.277 (+1.1% rel, less over-shrinkage toward the low global accept rate); optimum production €51,832,851 → €51,948,449 (**+€115.6K, +0.223%**), accepted cells 157 → 160 (+3), optimum risk 1.0900% → 1.0995% (+0.0095 pp, both under the 1.1% cap). Sub-1% headline impact, consistent with the M3 sensitivity finding for the decay/smoothing knobs.
- [x] **#5 — No-demand cells get the *smallest* RI uplift.** `reject_inference.py:apply_parceling_adjustment`. Repesca bins absent from the acceptance-rate table (zero demand = highest selection bias) were filled with the *median* observed rate → near-typical, anti-conservative multiplier. Fix (Option 3, domain-expert-chosen): **confidence-weighted shrinkage** toward a conservative low anchor — `rate_eff = conf·bin_rate + (1−conf)·anchor`, `conf = 1−exp(−n/scale)`, `anchor = max(low-percentile of observed rates, 0.01)` (fixed 0.05 if none). No-demand bins (conf 0) collapse to the anchor (high uplift); low-demand bins partially shrink (also fixes the adjacent over-trust of sparse bins, #37/#81); well-observed bins (conf ≈ 1) are ~unchanged. Shared `_shrink_acceptance_rate` helper is used by both `apply_parceling_adjustment` (runtime) **and** the RI optimizer's `_compute_calibration_error`, so the calibration objective scores candidates on the same rate definition runtime applies (lockstep — the optimizer had its own duplicate median-fill). Two config knobs `reject_no_demand_anchor_percentile` (0.10) / `reject_confidence_scale` (10.0); new `ri_effective_acceptance_rate` diagnostic. **Real-SAS before/after** (segment `no_premium_cd`, smoothing+decay on, RI optimizer off to isolate, identical trained model): no no-demand bins fired, so the effect is purely the low-demand de-trusting → swap-in reject multiplier avg 2.277 → 2.348 (+3.1% rel), swap-in risk 2.543% → 2.616%; optimum production €51,948,449 → €51,825,568 (**−€122.9K, −0.237%, more conservative**), accepted cells 160 → 157 (−3), optimum risk ~1.099% (unchanged, under cap). Sub-1%, consistent with M3.
- [x] **#6 — HurdleRegressor / zero-inflation rationale is inert.** `estimators.py`, `inference_optimized.py`, `optuna_tuning.py`. The two-part hurdle was trained on the bin-aggregated continuous `b2_ever_h6` (≈ never exactly 0), so the classifier learned all-ones and it collapsed to plain Ridge/Lasso while still offered as a distinct candidate; the zero-proportion diagnostic was measured post-aggregation (≈0) and never used. Fix (Option 3, domain-expert-chosen — make it meaningful): the hurdle is now trained on **per-loan** data where the default indicator has real zero mass, via a shared `_fit_hurdle_per_loan` adapter (per-loan `_hurdle_r` target + exposure weight `_hurdle_w`), and scored on the same bin-level CV RMSE as peers (same CV split → fair, no leakage). Exposure-weighted severity makes the cell prediction reconcile to the exposure-weighted aggregate `b2 = mult·Σnum/Σden` (exact in the no-shrinkage limit). Diagnostic now per-loan (`todu_30ever_h6<=0`). Gated behind `model_hurdle_per_loan` (**default off**) + a degenerate-zero-mass skip; default runs offer no hurdle (retires the inert candidate + 2 wasted Optuna tunings). **Real-data validation** (`no_premium_cd`, flag OFF vs ON): per-loan zero mass = 98.8% (vs the misleading post-aggregation ≈0); the per-loan hurdle now competes for real (Hurdle-Ridge CV RMSE 0.0206, classifier modeling the ~1.2% default rate) but **loses to LightGBM (0.0178)** → both runs select LightGBM, so the optimum is **IDENTICAL** (risk 1.0995%, production €51,948,449, 160 cells) — zero cutoff impact, and the surface-monotonicity risk never triggers (the hurdle doesn't win). Enabling in production remains an operational decision (M5 sign-off).
- [x] **#7 — Model selection reuses one holdout; 1-SE rule degenerates.** `inference_optimized.py`, `optuna_tuning.py`. Every model-type candidate (linear families AND trees) was scored on the **same** 0.2 holdout (same `random_state`), so all rows reported `CV Std RMSE = 0` → the 1-SE band (`best_mean + best_se`) collapsed to pure **argmin** (the complexity tiebreaker never fired), and that one reused holdout both *selected* the winner and reported its "unbiased" RMSE (winner's-curse bias). Fix (Option 1, domain-expert-chosen): score each candidate's tuned params with **fresh-seed k-fold CV** (mean + real SE, a different fold partition than tuning) → the 1-SE rule forms a proper band and prefers the simplest model within noise; the 0.2 holdout is kept **only** as a winner-only generalization report (`evaluate_holdout_rmse`, refit on 80% / scored on 20%, never used for selection; logged + in metadata). Deleted the per-tuning-function holdout + `eval_holdout_tree`/`_eval_holdout_linear`. **Real-data before/after** (`no_premium_cd`): the CV table now carries real SE (~0.0006–0.0008 vs the old 0.0); all candidates fall within LightGBM's 1-SE band, so selection flips **LightGBM → Linear Regression** (the simplest, statistically-tied model; winner held-out RMSE 0.0171). Optimum **1.0991% → 1.0899%**, production **€51.83M → €49.58M (−4.34%)**, accepted cells 157 → 129. **Material** (model flip + −4.3% production) — the largest of the Tier-1 RI/selection fixes and the correct, more-conservative effect of a working 1-SE rule; **requires M1/M5 sign-off before the new model drives live cutoffs.**
  - **Production (multi-segment) validation (2026-05-31): the −4.3% headline was a single-segment-training artifact, not the production impact.** In production all segments share ONE model trained on the pooled `total` supersegment (8 segments, ~50k bins), not the cd-only slice the headline was measured on. Re-running the *real* pooled training under pre-#7 (`767c708`) vs current code: pre-#7 (broken 1-SE, all CV-Std=0 → argmin) selects **Lasso α=0.001**; current (fresh-seed k-fold 1-SE) selects **Linear Regression** — **both linear, functionally near-identical** (α≈0 Lasso ≈ OLS). The alarming **LightGBM** pick was specific to the tiny cd-only training set and does **not** occur on pooled data. Base-scenario optima with each pooled model (segments ef→cd→ab chain + premium): no_premium_ef −2.93% (−€179K, −6 cells), no_premium_cd −0.88% (−€453K, −13 cells), no_premium_ab **0%** (fixed_cutoffs ⇒ production pinned; risk metric shifts ~+0.007pp), premium **0%** (optimum identical). **Aggregate −€632K, −0.416%**, all risk targets at/under cap, direction marginally more conservative. Conclusion: #7's production effect is a small, conservative shift between two near-equivalent linear models — **not** a regime change to a tree model. Lowers the operational risk of the original "material" flag (M1/M5 sign-off still advisable, but the live-cutoff impact is sub-1% in aggregate).
- [x] **#8 — TweedieGLM uses a penalized free `log(exposure)` coefficient, not a fixed offset.** `estimators.py`, `optuna_tuning.py`. The Tweedie converted the rate target back to the default amount and appended `log(exposure)` as a feature — but sklearn's `TweedieRegressor` L2-penalizes every coefficient, so the "offset" was a penalized free coefficient (shrunk off 1.0), not a true offset; and the same `todu_amt_pile_h6` was used as BOTH that exposure AND the `sample_weight` (double-counting), plus a predict-time median-exposure fallback. Fix (Option 1, domain-expert-chosen): model the rate `b2` directly with `sample_weight = exposure` — sklearn's documented exposure idiom, mathematically equivalent to `offset=log(exposure)` for the mean (exact for Poisson; ~exact for Tweedie 1<p<2). Exposure now used exactly once (as the weight); removed `exposure_col`/`multiplier` + `_split_exposure`/`_add_log_exposure`/`_median_exposure` + the exposure branches; `prepare_model_input` no longer appends an exposure column. Net simplification, consistent with how Ridge/Lasso/LightGBM model the rate. **Real-data before/after** (`no_premium_cd`): Tweedie CV RMSE 0.020587 → 0.020400 (corrected fit, marginally better); selected model **Linear Regression in both** (Tweedie doesn't win the 1-SE selection post-#7) → optimum **IDENTICAL** (1.0899%, €49,575,119, 129 cells) — **zero cutoff impact** on this segment.
- [x] **#9 — Cell-level CIs are too narrow.** `inference_optimized.py:compute_cell_level_ci`. Per-cell uncertainty used `SE = std(fold_preds)/√k`, treating cross-fold predictions as independent — but folds share training data, so they are positively correlated (the Nadeau–Bengio CV-variance pathology) and `/√k` underestimates the SE ~2×. Fix (Option 1, domain-expert-chosen): new `_nadeau_bengio_cv_se(std, n_folds)` helper returns the corrected `SE = std·√(1/k + 1/(k−1))` (`std/√k` guarded fallback below 2 folds); `compute_cell_level_ci` now uses it, keeping the `t(df=k−1)` critical value, the raw `pred_std` column, and the `<2-fold → NaN` branch. **Scope (verified): these CIs are report-only** — no decision path consumes them (not the optimizer, monotonicity relaxation, scenario selection, or any cutoff rule); they feed the dashboard heatmap/CSV display only. The cutoff-level bootstrap (`calculate_bootstrap_intervals`) and consolidation aggregation CIs are separate and unchanged. **Real-data before/after** (`no_premium_cd`, 163/180 cells with a CI): `pred_mean`/`pred_std` bit-identical (same deterministic model); CI half-widths widen by exactly the NB/naive factor `√(1 + k/(k−1))` per cell's fold count — k=2 ×1.732 (23 cells), k=3 ×1.581 (54), k=4 ×1.528 (86); mean half-width 0.00604 → 0.00987. Optimum **IDENTICAL** (1.0899%, €49,575,119, 129 cells) — **zero cutoff impact**, just more honest (wider) cell uncertainty in the display.
- [x] **#10 — SPC control limits not estimated from within-subgroup variation.** `trends.py:detect_trend_changes`, `main.py:601`. The control-band σ was the rolling **MAD of the raw levels** (`rolling_mad/0.6745`), which captures the full within-window dispersion *including any trend* — so under a slow drift the spread inflated, the band widened, and anomalies riding the drift were **masked**. Compounded by a tertiary **global-std** fallback (worst estimator under drift), a small-window **t-quantile × per-point-spread** branch (at `window=3`, df=2 → band explodes), and `main.py` calling it with **`window=3`** (2 moving ranges). Fix (Option 1, domain-expert-chosen): replace with the robust **Individuals moving-range (I-MR)** estimator `σ̂ = median(|x_i − x_{i-1}|)/0.9539` — within-subgroup short-term variation, drift-immune; keep the lagged robust-median centre and the no-leakage `.shift(1)`; use a plain Shewhart `n_sigma` multiplier (dropped the t-quantile branch **and** the global-std fallback); raise the production window **3 → 6**. Median-MR (not mean-MR/1.128) to resist a single outlier month on short (~12-pt) series and match the file's robust style. Methodology doc bullets updated. **Scope (verified): report-only** — `is_anomaly`/`direction` flow only to a CSV + WARNING log (non-blocking) + the HTML "Anomalies" table; no optimizer/monotonicity/scenario/cutoff path consumes them. **Real-data before/after** (`no_premium_cd`, 37 months): mean band half-width **0.1158 → 0.0273** and far more stable (old window=3 bands swung 0.005–0.257; new sit in 0.013–0.031); the drift-masking bug is demonstrably fixed — 2024-02 (0.109) & 2024-03 (0.114) had old half-widths of **0.257** (drift-inflated) ⇒ masked, now flagged; 2024-12 similarly surfaced; spurious window=3 flags (2023-08/10, 2025-02/03) drop out. Optimum **byte-IDENTICAL** (1.089877%, €49,575,118.89, 129 cells) — **zero cutoff impact**. 3 new tests (drift-masking regression, constant-series, MR-σ unit) + 1164 pass.
- [x] **#11 — PSI "non-standard" epsilon-only-in-log variant — OVERSTATED, verified defensible (no formula change).** `stability.py:calculate_psi`/`calculate_csi_for_categorical`, `metrics.py:calculate_psi_by_period`. PSI = `Σ (a−e)·ln(a_safe/e_safe)` with epsilon flooring only the log term (difference keeps true proportions, no re-normalization). The audit's specific worry — *asymmetric weighting of appearing vs disappearing bins* — **does not hold**: PSI is invariant under swapping the two populations (`PSI(a,e)=Σ(a−e)ln(a/e)=PSI(e,a)`), and swapping turns every appearing bin into a disappearing one, so the two are weighted **identically** (numerically confirmed: appearing-mass-p == disappearing-mass-p, e.g. 0.6215==0.6215 at p=0.05). It also equals the textbook epsilon-in-**both**-terms PSI to **O(ε)** — diff ≈ `ε·ln(p/ε)` per empty bin (~1.5e-3 in tests), far below the 0.1/0.25 thresholds, which therefore remain valid; and every term is ≥0 (PSI≥0). Fix (Option A, domain-expert-chosen): **keep the formula**, encode the verification — added `test_psi_symmetric_under_population_swap`, `test_psi_matches_textbook_within_epsilon`, `test_csi_symmetric_under_population_swap`, and a docstring note in all three functions. **Report-only** (PSI/CSI → CSV/JSON/HTML + INFO/WARNING logs + alert severities; `mr_pipeline` even downgrades errors to warnings; no optimizer/cutoff path consumes them). No formula change ⇒ **PSI values byte-identical, zero cutoff impact**; no real-data run needed. Existing golden/empty/threshold tests unchanged & green; suite 1167 pass. (Mirrors #2/#3: original HIGH framing overstated on close reading.)
- [x] **#12 — PSI silently returns ~0 for low-cardinality scores.** `stability.py:calculate_psi`/`get_psi_status`, `metrics.py:calculate_psi_by_period`. `pd.qcut(..., duplicates="drop")` silently collapses to a single bin on a low-cardinality / dominant-value score (it does **not** raise, so the `except ValueError → equal-width` fallback never fired); one bin makes `PSI=(1−1)·ln(1)=0` regardless of any real shift → false **"stable"**. Fix (Option A, domain-expert-chosen — *measure it properly; NaN only when constant*): the int-bins path now branches on baseline cardinality — `nunique<2` (constant reference) → **NaN + warning** (never silent 0); `nunique<=bins` (discrete) → **one bin per distinct value** (a real discrete-support PSI); else `qcut`, and a silent `<2`-bin collapse → **equal-width fallback** (NaN if still degenerate); every fallback logs a warning. `get_psi_status(NaN)` now explicitly → `UNSTABLE` (surfaced for investigation, never silently STABLE). Parallel hardening in `metrics.calculate_psi_by_period`: `np.unique` the percentile breakpoints (low cardinality made `np.histogram` crash on duplicate edges) + NaN guard. **Report-only** (no optimizer/cutoff path consumes PSI). **Real-data before/after** (`no_premium_cd`): the monitored continuous vars (`oa_amt`/`risk_score_rf`/`score_rf`) take the unchanged qcut path → `stability_psi*.csv` **byte-identical** (PSI 0.0775/0.0512/0.0064), optimum **byte-IDENTICAL** (1.089877%, €49,575,118.89, 129 cells) ⇒ **zero cutoff impact**; the new low-card behavior is exercised by 4 new unit tests (discrete-shift detected, dominant-value fallback, constant→NaN, metrics no-crash). Suite 1171 pass.
- [x] **#13 — global_optimizer Pareto pruning unified on the canonical sweep (was: "fragile").** `global_optimizer.py:load_frontier`, `optimization_utils.py`. Two Pareto-pruning implementations existed: `load_frontier` used `cummax(oa_amt_h0)` + a hard-coded **1e-4** epsilon; `trace_pareto_frontier` used the canonical strict `prod > prev_max` (no epsilon). Same rule modulo the epsilon — and since production is in **euros (~10⁷)**, a sub-1e-4 (0.01-cent) gap never occurs between distinct solutions, so the epsilon **never fired** on real data. Fix (Option A, domain-expert-chosen): extracted the canonical rule into a shared `pareto_keep_indices(production_sorted_by_risk)` helper and call it from **both** — `trace_pareto_frontier` (pure refactor, identical logic ⇒ per-segment pipeline unchanged) and `load_frontier` (drops the cummax/1e-4 mask). Single source of truth, magic number gone. The global frontier **is** the allocation-MILP candidate set (first non-report-only fix), but **real-data `run_allocation` before/after** (11 segments @ target 1.2%, multi-segment `output/` frontiers): per-segment pruned point counts **identical**, global risk **1.1997%**, global production **€297,093,790**, and the allocation CSV + policy-cutoff table (chosen `sol_fac`/risk/production/cells/swap per segment) are **byte-IDENTICAL** ⇒ **zero allocation impact**. 3 existing pruning golden tests stay green; +1 documents the new strict-`>` (sub-epsilon gain now kept) +6 `pareto_keep_indices` unit tests. Suite 1178 pass. (Audit's "fragile" overstated — the old filter was correct; this is a consistency/maintainability fix.)
- [x] **#15 — MILP decode could report an infeasible allocation as optimal.** `global_optimizer.py:optimize_exact`/`optimize_greedy`/`AllocationResult`, `run_allocation.py`, `portfolio_owner.py`. After `scipy.milp` succeeded, the decode picked each segment via `argmax(seg_x)`; on a degenerate/fractional result (`round(seg_x).sum() != 1`) it only warned and still took the argmax, **never re-checking** the chosen rows vs the global risk target / per-segment bounds, and returned an `AllocationResult` with **no feasibility field** → infeasible presented as optimal (and binding-constraint slack was computed on the *fractional* solver vector, masking it); `optimize_greedy` likewise could stop with `risk > target` and return silently. Fix (Option A, domain-expert-chosen — validate+raise+honest flag end-to-end): new `_check_allocation_feasible()` re-validates the **decoded** integer choices against every MILP constraint (global production-weighted risk ≤ target, per-segment min/max risk + min production, global production floor); `optimize_exact` **raises** on violation → routed through `optimize()`'s existing greedy fallback; `AllocationResult` gains `feasible`/`message` set by **both** methods (greedy now honestly labels its best-effort instead of silent `risk>target`); binding constraints recomputed from the decoded one-hot; `run_allocation` logs a loud WARNING and the portfolio-owner narrative carries an `⚠ INFEASIBLE` line. **Correctness guard** (the argmax path is rare since scipy returns clean integers on success), allocation-path but **not** report-only. **Real-data `run_allocation` before/after** (`output/`, 11 segments): **feasible** target 1.2% → allocation CSV + policy-cutoff table **byte-IDENTICAL**, `feasible=True` (zero impact on normal runs); **infeasible** target 0.3% → exact infeasible → greedy fallback (best 1.0192%) now **flagged `feasible=False`** with a loud WARNING + narrative INFEASIBLE line (was silently presented as optimal). +5 `_check_allocation_feasible` unit tests, +2 greedy-flag tests, existing exact/greedy/locking tests carry `feasible` asserts; suite 1185 pass.
- [x] **#16 — Monotonicity-direction inference mis-calibrated + wrong default.** `preprocess_improved.py:_infer_monotonicity`. For each variable **without** an explicit `[preprocessing.directions]` entry, it inferred risk's ascending/descending direction by `sign(weighted_cov)` **only when a permutation test was significant** — and on non-significance (p>0.10) or cov≈0/NaN it **discarded the empirical sign and forced `direction=1` (ascending)**, backwards for credit scores (higher score = safer = `-1`). The permutation also held production **weights fixed** while shuffling risk ranks → mis-calibrated p-value that deemed genuinely-descending-but-noisy scores "not significant," amplifying the wrong default. Direction feeds `inv_vars` → a **hard MILP monotonicity constraint**. Fix (Option A, domain-expert-chosen — empirical sign always + calibrated warning): new pure `_infer_direction_from_bins()` sets `direction = sign(rho_w)` of a coherent production-weighted rank correlation **regardless of significance**; the permutation (now permuting the **(risk_rank, weight) pairs jointly** → exchangeable null) is **confidence-only** and never overrides the sign; a flat/degenerate profile (`rho_w` NaN/≈0) → documented ascending default + loud warning. **Blast radius:** inference is **dormant** when directions are set explicitly (standard production path). **Real-data on `no_premium_cd`:** (a) standard config (dirs explicit) before/after → optimum **byte-IDENTICAL** (1.0899%, €49,575,119, 129) — zero impact on normal runs; (b) **directions stripped to force inference** → the fix infers **−1/−1/−1** (correct descending; rho_w −0.47/−0.51/−1.00, all "weak" p>0.10) and reproduces the **explicit-directions optimum** (1.0899%, €49.575M, 129), whereas the **old code forced +1/+1/+1 (ascending)** → backwards constraints → optimization **crashed** (`MILP and GA produced no solutions`). +6 unit tests incl. the noisy-descending regression (old would force +1, new returns −1). Suite 1191 pass. (No `analyze_logs` regex couples to the new colon-formatted log wording.)
- [x] **#17 — RI partial-order monotonicity "fix" was not a valid isotonic projection.** `reject_inference.py:_fix_partial_order_violations`/`apply_parceling_adjustment`. The post-hoc that enforces monotonicity on the per-cell `reject_risk_multiplier` (over the N-D bin poset; gated by `reject_enforce_monotonicity`, **on** in config.toml/ri_verify.toml; multiplier scales `todu_30ever_h6` → optimizer, **not** report-only) **averaged violating pairs** in a `for _ in range(5)` loop — averaging a pair can re-break others, the loop could exit non-converged on deep posets, and it produced an arbitrary monotone *staircase* rather than the L2 projection. Fix (Option A, domain-expert-chosen — block-pooling generalized PAVA): pool whole connected violating **blocks** via union-find to their mean and iterate to convergence — blocks only ever merge ⇒ **guaranteed termination with zero residual partial-order violations** and the correct isotonic surface (e.g. a decreasing chain now pools to its mean, not a staircase). Kept the axis-wise sklearn isotonic warm-start; reordered to **clip-then-enforce** so the output is provably **both monotone and within [1, max_mult]** (a uniform clip is order-preserving — the audit's "clip re-breaks monotonicity" sub-claim was unfounded; noted, not "fixed"). **Real-data before/after** (`no_premium_cd`, monotonicity on): the post-hoc fires on real data (old averaged 17 violating pairs; new pooled 9 blocks) yet the optimum is **byte-IDENTICAL** (1.0899%, €49,575,119, 129) ⇒ **zero cutoff impact** — the old averaging happened to reach an adequate surface here; the fix's value is the guarantee + correctness. +5 unit tests (zero-residual-violation guarantee, chain→isotonic-mean, idempotence, inverted axis, bounded+monotone after clip-then-enforce). Suite 1196 pass.
- [x] **#14 — Global risk production-weighted vs segment risk exposure-weighted — convention surfaced + cross-checked (definitional, no optimizer change).** `global_optimizer.py`, `run_allocation.py`, `portfolio_owner.py`, README. The global allocation optimizes a **production-weighted** average of segment bad-rates (`Σ prod·risk/Σ prod`, risk per euro of new production — the documented intent), while each segment's `b2_ever_h6` is **exposure-weighted** (`Σbad/Σexposure`); the two differ only if production ∦ exposure. Confirmed **documented-intentional**, not a bug (matches the audit's "definitional, confirm with business"). Fix (Option A, domain-expert-chosen): **keep the production-weighted constraint unchanged** (zero cutoff/allocation impact) and additionally compute + report the **exposure-weighted portfolio bad-rate** (`Σ b2·exposure/Σ exposure`, from `todu_amt_pile_h6` already present per frontier point) on `AllocationResult.portfolio_bad_rate`, in `__str__`, `run_allocation`, and the portfolio-owner narrative; documented the convention (README + docstring). None-safe for old frontiers lacking exposure. **Real-data `run_allocation`** (`output/`, 11 segments @ target 1.2%): allocation CSV + policy-cutoff table **byte-IDENTICAL** to `main` ⇒ zero impact; the two bases are **near-identical on this portfolio** — production-weighted **1.1997%** vs exposure-weighted **1.2005%** (0.0008pp gap), confirming the convention difference is immaterial here. +3 unit tests (exposure-weighted reported & differs; allocations unchanged with/without exposure; greedy reports it). Suite 1199 pass.

### Tier 3 — Data quality & loading (fail-open / silent coercion)

- [x] **#18 — DQ checks no longer fail open on genuine corruption + booked-ratio denominator fixed.** `data_quality.py`, `config.py`, `pipeline/preprocessing.py`. DQ "failed open": negative counts/amounts and unparseable dates were **WARNING-only** (warnings never halt — the gate stops only on `is_valid=False`/`FAILED`, and the dead `allow_warnings` knob was unwired), and `check_booked_ratio` divided by the **full pre-segment-filter** frame (a *global*, all-segment ratio incl. fraud/legal). Fix (Option A, domain-expert-chosen — fail-closed for corruption + strict knob): negative indicator values (`check_indicator_values`) and date-parse failures (`check_date_range` except branch) → **FAILED** (halt by default); coverage gaps + outliers stay **WARNING** (legitimately variable). `check_booked_ratio` now takes `segment_filter` and computes on the **segment demand population** (regex/supersegment-aware match + `fuera_norma`/`fraud_flag`/`nature_holder!=legal` exclusions). New config **`dq_allow_warnings` (default True)** wired into the preprocessing gate so operators can opt into strict mode (warnings also halt). **Probed real data: clean** (0 negatives across 7 indicators; 100% of `mis_date` parses) ⇒ fail-closing is **zero-impact on current runs**. **Real-data DQ on `no_premium_cd`**: run passes (0 failures), and the booked ratio is now the correct **per-segment 14.2% (20,297/143,269)** vs the old **global 7.0% (101,123/1,444,802)**; DQ only gates (never mutates) ⇒ optimum unchanged. +6 tests (negatives→FAILED, unparseable→FAILED, segment denominator + demand exclusions + regex filter, strict-mode halt). Suite 1204 pass.
- [x] **#19 — String coercion now protects dates/IDs + SAS encoding unified (latent-robustness + consistency; no result change).** `data_manager.py`, `run_batch.py`, `config.py`. Both load paths blanket-coerced every object/string column (`lower`+`_`+`category`). This is **essential** — probed raw SAS is Title/UPPER case (`status_name`=`Booked`, `reject_reason`=`09-SCORE`, `fuera_norma`=`N`, `se_decision_id`=`OK`), and every downstream filter depends on the lowercased form — but the blanket loop **would mangle** a string date (space→`_` breaks `2024-01-01 00:00:00`) or case-sensitive ID. On current data it **corrupts nothing** (`mis_date`=datetime64, `authorization_id`=numeric → not object; `se_decision_id` is string but genuinely a filter), so the audit's "corrupts" was overstated → **latent** risk. Fix (Option A, domain-expert-chosen — protect dates/IDs + unify encoding): shared `standardize_columns_and_values()` helper (used by both `load_and_prepare_data` and `run_batch.load_and_standardize_data`) **skips** `mis_date`/`authorization_id`/any `*_date` from value coercion while still normalizing the categorical filters (`se_decision_id` stays normalized). New config **`sas_encoding` (default `latin-1`)** used by both read sites, replacing `run_batch`'s hard-coded `utf-8` (removes the latin-1/utf-8 split). **Real-data: standardized frame byte-IDENTICAL** main vs branch (filter cols + `mis_date`/`authorization_id` fingerprints all match — protected cols are non-object so the new skip is never reached) ⇒ **zero result impact, optimum unchanged**; and `run_batch`'s load path now produces **identical** standardized data to the main path. +4 tests (filter cols normalized; string-date/ID/`*_date` left verbatim; col-name lowercasing; encoding plumbed from settings). Suite 1208 pass.
- [x] **#20 — Required-column check unified on a shared case-sensitive helper (no false assurance; no result change).** `data_manager.py`, `data_quality.py`. `check_required_columns` (DQ) was **case-insensitive** while `validate_data_columns` (the hard gate) is **case-sensitive** — so DQ could report "all columns present" on mixed-case data that the case-sensitive pipeline (and pandas `df[col]` access) actually rejects (**false assurance**). Both run post-standardization on lowercased names in the live flow, so the divergence was **latent** (only the standalone `validate_data_or_fail`, never called by the pipeline, could hit it). Fix (Option B, domain-expert-chosen — unify): new shared `find_missing_columns(df, required)` (**case-sensitive** exact match — the pipeline's real requirement) backs **both** functions, so "passes DQ" ⟺ "passes validate" by construction and they can't diverge again. **Real-data:** DQ output is **byte-IDENTICAL** main vs branch (Required Columns PASSES on both; the unrelated standalone-config failures are identical) ⇒ **no-op in the live flow**. Behavior change is only that DQ now correctly FAILS on non-standardized mixed-case columns instead of falsely passing. +4 tests (DQ case-sensitive exact-match; `find_missing_columns` present/missing/case-sensitive). Suite 1211 pass.
- [x] **#21 — Out-of-range binning now flags OOD-snapped vs NaN-dropped distinctly (already warned; overstated).** `preprocess_improved.py:_apply_binning_from_config`. `pd.cut` NaNs scores outside the edges; the code snaps below-range→first bin / above-range→last bin (KEPT — these contaminate the boundary cell that feeds the MILP grid) and drops true-NaN-source rows. It **already** logged a WARNING + **hard-fails >1%**, so "silently snapped" was **overstated**. Gap: the warning **conflated** OOD-snapped (kept) with NaN-source (dropped), and no test covered the finite-edge path. Fix (Option A, domain-expert-chosen — clarify the flag, keep behavior): one WARNING now reports `N below-range + M above-range snapped to boundary bins` vs `K NaN-source dropped` separately (precomputed `n_nan_src = nan_count − n_below − n_above`). Snapping + the 1% hard-fail are **unchanged**. **Blast radius: unreachable on the standard config** — `octroi_bins`/`efx_bins` and learned/quantile edges are always `±inf`-capped ⇒ `nan_count==0` ⇒ the block is skipped ⇒ **byte-identical output** (no-op); reachable only via a finite `bin_edges` override. +4 tests exercise the finite-edge path (below→first bin, above→last bin, NaN-source dropped & distinct, >1% raises). Suite 1215 pass. (Mirrors #11/#13/#19/#20: latent / overstated → clarity + test-coverage fix.)

### Related sub-findings (lower priority / context)

- [x] **#2b — Immature zero-H6 rows: maturity-filter silent skip now flagged (mitigated-by-default; logging-only).** `mr_pipeline.py:_compute_hybrid_mr_risk`. The `.notna()` gate admits immature rows with `todu_30ever_h6==0.0` (0 isn't NaN — zero only because unseasoned); the #2 maturity filter excludes them, but **only** when `mr_maturity_months>0` AND `mis_date` present, and the block was **silently skipped** otherwise → immature zeros dilute `b2_mr` (which feeds the MR optimization mask, decision-critical). **Defaults already mitigate it** (`use_mr_outcomes=False` off by default; `mr_maturity_months=6`>0 when on). Fix (Option A, domain-expert-chosen — make exclusion reliable when requested + warn; respect the `0=disable` knob): added the missing branches — `mis_date` absent + `mr_maturity_months>0` → loud **ERROR** (requested maturity filter could not run; immature zeros may understate MR risk); `mr_maturity_months==0` → **INFO** (filtering disabled, only use 0 on a fully-mature MR window). **Logging-only** ⇒ standard config (maturity=6, mis_date present) runs the unchanged first branch → `b2_mr` identical, optimum unchanged. +2 tests (immature H6=0.0 excluded when filter active & `b2_mr` undiluted — fills the prior coverage gap; missing-`mis_date` path warns + counts all). Suite 1219 pass. (Audit's "treat as NaN?" → yes when filtering is requested; respect the explicit disable.)
- [x] **RI Gini imputation self-fulfilling — labeled as a circular upper bound (diagnostic-only; value unchanged).** `selection_bias.py:compute_ri_gini`, `selection_bias_plots.py`, `run_selection_bias_analysis.py`. Confirmed: rejected bad outcomes are imputed as `Binomial(booked_bad_rate[score_decile]·uplift)` — a monotone function of the **same score** the Gini then ranks by — so `ri_gini_mean` is **mechanically inflated** by construction. Verified **diagnostic-only** (CSV `ri_gini.csv` + the "RI-imputed" plot bar + a summary print; **nothing** consumes it — no cutoff/RI-tuning/model-selection/gate → zero result impact). The docstring already admitted "inherently circular" but the user-facing output didn't warn readers. Fix (Option A, domain-expert-chosen — keep the metric, make the circularity loud): added a `note` field to the returned dict (flows into `ri_gini.csv`), relabeled the plot bar to **"RI-imputed (circular upper bound)"** + a chart footnote, a **CAUTION** line in the summary printout (point to the Thorndike-corrected Gini instead), and strengthened the docstring. **Value unchanged** ⇒ no behavior/cutoff impact. +1 test (result carries a "circular" note); suite 1216 pass.
- [x] **Thorndike `attenuation_pct` negative — clamped corrected ≥ observed (defensive; unreachable for valid inputs).** `selection_bias.py:thorndike_case2_correction`. A Case-2 range-restriction correction (`u<1`) only *raises* the estimate (`r_corr ≥ r_obs` since `denom=sqrt(u²+r²(1−u²))≤1`), so `corrected_gini < observed_gini` → negative `attenuation_pct` should never happen. Investigation found it's actually **unreachable for valid Gini ∈ [0,1]**: the `np.clip(r_corr, 0, 0.999)` ceiling maps to `_r_to_auc(0.999)=1.0` ⇒ `corrected` saturates at **1.0 ≥ any valid observed** (only an invalid `observed_gini>1` inverts) — so the audit's "can be negative" was overstated/latent. Fix (Option A, domain-expert-chosen): added `corrected_gini = max(corrected_gini, observed_gini)` to make the theory invariant explicit + robust (e.g. if the clip bounds change later); `attenuation_pct ≥ 0` follows. **No-op on valid inputs** (corrected already ≥ observed); diagnostic-only (feeds the selection-bias plot/CSV, not the optimizer) ⇒ zero cutoff impact. +1 invariant test (corrected ≥ observed & attenuation ≥ 0 across an observed_gini × u_ratio grid incl. near-saturation). Suite 1217 pass.
